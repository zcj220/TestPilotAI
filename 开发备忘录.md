# TestPilot AI 开发备忘录

> 最后更新：2026-03-17（v13.2 官网HTTPS上线 | 下载页 | 扩展UI修复 | 坑23-26）
> 项目结构详见 README.md，此处不重复。

---

## 一、新电脑环境搭建

### macOS 环境搭建（✅ 2026-03-15 验证通过）

#### 前置安装
```bash
# 1. 安装 Python 3.12（项目要求 >=3.10,<3.13，macOS 系统 Python 3.14 不兼容）
brew install python@3.12

# 2. 安装 Poetry
curl -sSL https://install.python-poetry.org | /opt/homebrew/bin/python3.12 -
# 将以下加入 ~/.zshrc 或临时使用：
export PATH="/Users/zcj/.local/bin:$PATH"

# 3. Node.js 18+（brew 安装或 https://nodejs.org）
brew install node
```

#### 项目初始化
```bash
cd /Users/zcj/Desktop/Projects/TestPilotAi
poetry env use /opt/homebrew/bin/python3.12
poetry lock   # pyproject.toml 改过时需要重新生成
poetry install
poetry run playwright install chromium
cp .env.example .env   # 编辑 .env 填入 API 密钥，并确保 TP_SERVER_RELOAD=false
poetry run python main.py  # 验证：访问 http://127.0.0.1:8900/api/v1/health
```

#### IDE 插件安装（VS Code / Windsurf / Cursor）
```bash
cd extension
npm install && npm run compile
npx vsce package --allow-missing-repository
# VS Code
"/Applications/开发工具/Visual Studio Code.app/Contents/Resources/app/bin/code" \
  --install-extension testpilot-ai-1.0.0.vsix
# Windsurf
"/Applications/开发工具/Windsurf.app/Contents/Resources/app/bin/windsurf" \
  --install-extension testpilot-ai-1.0.0.vsix
# Cursor
"/Applications/开发工具/Cursor.app/Contents/Resources/app/bin/code" \
  --install-extension testpilot-ai-1.0.0.vsix
```

#### MCP 配置（Mac 路径版）
```bash
# 查询 Poetry 虚拟环境 Python 路径
poetry run python -c "import sys; print(sys.executable)"
```

**Windsurf**：`~/.codeium/windsurf/mcp_config.json`
```json
{
  "mcpServers": {
    "testpilot": {
      "command": "/Users/zcj/Library/Caches/pypoetry/virtualenvs/testpilot-ai-ZsVDSK5A-py3.12/bin/python",
      "args": ["/Users/zcj/Desktop/Projects/TestPilotAi/run_mcp.py"],
      "env": {}
    }
  }
}
```

**Cursor**：`~/.cursor/mcp.json` 中 `mcpServers` 里添加同样的 `testpilot` 条目。

#### iOS 测试支持（macOS 专属，需要 Xcode）
```bash
# 安装 Appium 及双平台驱动
npm install -g appium
appium driver install uiautomator2   # Android（复用原有功能）
appium driver install xcuitest       # iOS（macOS 独有）

# 连接 iPhone，启动 Appium
appium

# 查看已连接的 iOS 设备（需 Xcode + Developer Mode 开启）
idevice_id -l        # 需要安装 libimobiledevice
xcrun xctrace list devices
```

**iOS 连接 iPhone 步骤**：
1. iPhone 设置 → 隐私与安全 → 开发者模式 → 打开
2. Mac 用 USB 连接，Trust 信任
3. `appium driver install xcuitest` 已完成
4. 启动 Appium，在 TestPilot 前端的「手机」Tab 连接设备（platform=iOS）

#### macOS 桌面应用测试（Tauri）
```bash
# 安装 Rust
brew install rustup-init && rustup-init
rustup target add aarch64-apple-darwin

cd desktop && npm install
cargo install tauri-cli
cargo tauri dev    # 开发预览
cargo tauri build  # 打包 .dmg（不需要 Windows 的 NSIS）
```

> ⚠️ Mac 注意事项：
> - `TP_SERVER_RELOAD=false`（与 Windows 相同，Playwright 子进程需要）
> - `pywinauto` 仅 Windows 安装（pyproject.toml 已加 `sys_platform == 'win32'` 限制）
> - `desktop.py` / `window_manager.py` 的 `ctypes.windll` 已做平台判断，Mac 不会崩溃
> - 桌面控制器（Win32 API）在 Mac 上不可用，仅 Web/Android/iOS 可用

---

### Windows 环境搭建（原记录保留）

#### 前置安装
- **Python 3.10+**: https://www.python.org/downloads/
- **Poetry**: `pip install poetry`
- **Node.js 18+**: https://nodejs.org/（前端构建用）
- **Rust + Cargo**: `winget install Rustlang.Rustup`（桌面应用打包用）
- **Visual Studio 2022 C++ Build Tools**（Tauri编译需要）

#### 项目初始化

```powershell
cd "D:\Projects\TestPilotAI"
poetry config virtualenvs.in-project true
poetry install
poetry run playwright install chromium
copy .env.example .env   # 编辑 .env 填入 API 密钥
cd desktop && npm install && npm run build && cd ..
poetry run python main.py  # 验证：访问 http://127.0.0.1:8900
```

#### Tauri 桌面应用
```powershell
cargo install tauri-cli
cd desktop
cargo tauri dev    # 开发模式
cargo tauri build  # 打包exe（需要先排除Windows Defender扫描target目录）
```

---

## 二、双电脑同步

### Git（推荐）
```bash
git add . && git commit -m "进度说明" && git push
# 另一台电脑
git pull && poetry install && cd desktop && npm install && npm run build
```
- GitHub用户名：zcj220（私有仓库待创建）
- 备用方式：双击 `backup.bat` 生成zip拷贝

### 到新电脑后
1. `git clone` 或复制项目到 `D:\Projects\TestPilotAI\`
2. `poetry install && poetry run playwright install chromium`
3. `cd desktop && npm install && npm run build`
4. 首次Tauri编译约3分钟，后续增量编译很快

---

## 三、常用命令

| 操作 | 命令 |
|------|------|
| 启动服务 | `poetry run python main.py` |
| 热重载开发 | `poetry run uvicorn src.app:app --reload --port 8900` |
| 运行测试 | `poetry run pytest` |
| 测试+覆盖率 | `poetry run pytest --cov=src` |
| 一键备份 | 双击 `backup.bat` |
| 安装新依赖 | `poetry add 包名` |

---

## 四、AI 配置

- **模型**：Doubao-Seed-1.8（doubao-seed-1-8-251228）— 文本+视觉+Agent统一模型
- **调用方式**：OpenAI SDK，方舟平台API完全兼容
- **配置文件**：`.env` 中以 `TP_AI_` 为前缀
- **关键变量**：
  - `TP_AI_API_KEY` — 方舟平台API密钥（UUID格式）
  - `TP_AI_BASE_URL` — `https://ark.cn-beijing.volces.com/api/v3`
  - `TP_AI_MODEL` — `doubao-seed-1-8-251228`

---

## 五、当前开发进度

| 版本 | 内容 |
|------|------|
| v0.1-v1.0 | ✅ 基础骨架→测试执行→交叉验证→自动修复→IDE插件→前端→可视化→商业化 |
| v1.1 | ✅ 蓝本模式（Blueprint+解析器+执行器+AI视觉验证） |
| v1.2 | ✅ MCP Server（5个工具，编程AI双向闭环） |
| v1.3 | ✅ 异常检测+智能输入+快速探索+智能修复+记忆激活 |
| v1.4 | ✅ 公式验证+AI周期压缩+积分精算 |
| v2.0 | ✅ Web仪表盘+终端日志+测试控制+循环模式+多页面+context池 |
| v2.1 | ✅ 截图推送+蓝本前端入口+Tauri桌面应用(1.75MB)+日志智能切片 |
| v2.2 | ✅ 桌面应用内嵌Python引擎（Tauri IPC启停+自动清理进程） |
| v2.3 | ✅ IDE插件打磨（蓝本模式Tab+测试控制+截图预览+端口修正→v1.0） |
| v3.0 | ✅ 环境管理页（本机模式默认+Docker可选折叠）+ 三AI交叉验证增强 |
| v4.0 | ✅ CI/CD自动化（CLI工具+GitHub Actions+Dockerfile+Webhook通知） |
| v5.0 | ✅ 手机测试（多平台控制器+Appium+API+CLI+截图+AI分析+前端集成）— 实机验证通过(vivo V2243A) |
| v5.1 | ✅ 手机测试增强（蓝本权限+厂商弹窗库+Docker模拟器+控制台收集+网络错误收集） |
| v5.2 | ✅ 报告分析可视化（通过率趋势+Bug热力图+历史对比+HTML导出+前端页面） |
| v6.0 | ✅ 用户系统+商业化基础（JWT鉴权+项目空间+用量计量+SQLAlchemy双驱动+仪表盘） |
| v6.1 | ✅ 团队协作（团队空间+邀请加入+角色权限+项目共享+团队看板+前端页面） |
| v7.0 | ✅ 桌面应用测试（DesktopController+WindowManager+Win32 API+PS UI Automation+11个API+前端页面） |
| v8.0 | ✅ 微信小程序测试（MiniProgramController+Node.js桥接+miniprogram-automator+11个API+前端页面） |
| v9.0 | ✅ 多端协同测试Phase1-3（协调器+蓝本+AI扮演+录制回放+一致性+网络模拟+设备池+37个API+前端5视图） |
| v10.0 | ✅ Preview静态文件服务器（`/preview/<app>/` 直接访问本地demo应用） |
| v10.1 | ✅ IDE插件蓝本生成重设计：<br>**废弃**：URL→AI爬取HTML→生成蓝本（原理缺陷：看不到JS逻辑/未触发状态/容易超时）<br>**新方案**：插件提供「📋 复制蓝本生成提示词」按钮 → 用户粘贴到Cursor/Windsurf → 编程AI读源码生成精准蓝本<br>核心哲学：**编程AI是第一负责人**，TestPilot只做执行器 |
| v10.2 | ✅ 蓝本生成提示词13条黄金规则（含深度Bug挖掘+功能文档参考+截图策略+功能模块拆分+增量更新）|
| v10.3 | ✅ **截图策略修复+Session管理**（commit 2115af2）：<br>**Bug修复**：引擎每步都截图→改为按需截图（仅蓝本写了screenshot或有expected时）<br>**Session清理**：安卓项目切换后旧session残留→自动检测失效并清理<br>**蓝本拆分验证**：mobile-demo拆为5个功能模块蓝本，PC测试249/251通过(99.2%) |
| v12.0 | ✅ **AI中枢决策层AIHub**（commit 85b0481）：跨平台统一AI大脑，L0直通→L1弹窗自愈→L2失败诊断→L3场景熔断，Bridge模式接入三大Runner |
| v12.1 | ✅ **L2升级：截图+步骤历史**（commit bea4c10）：StepRecord自然语言描述，L2发截图+历史问AI像人一样分析，三Runner统一record_step |
| v12.2 | ✅ **归因预判FaultType**（commit 6f634e1）：L0.5规则预判零token判定APP Bug，L2提示词加fault归因，Bug标签[应用Bug]/[测试问题] |
| v13.0 | ✅ **社区经验库+商业化基础设施**（2026-03-15完成A/B/C/D）：<br>**A**✅：社区数据库6张新表+23个API端点+Alembic迁移+PostgreSQL驱动<br>**B**✅：社区Web门户（React+TailwindCSS+i18n中英切换，8个页面：GitHub风格UI）<br>**C**✅：匿名化+审核+评分+分享预览+自动推荐+举报机制（30个新测试全过）<br>**D**✅：Docker Compose+积分拦截中间件+支付Webhook+管理后台API<br>**E**：Mac/iOS平台支持（Mac上并行开发，Git合并无冲突） |

- 测试总数：**962个**，全部通过
- GitHub仓库：`https://github.com/zcj220/TestPilotAI`（已上传）
- 引擎端口：**8900**（统一）
- IDE插件版本：**1.0.0**（extension/，VSCode/Windsurf通用）
- CLI工具：`python cli.py run -b testpilot.json -o report.xml -f junit`
- 手机测试：`python cli.py mobile devices` / `mobile connect` / `mobile screenshot`
- 三AI架构：**AI-A测试 + AI-B数据生成 + AI-C审查仲裁**
- Docker沙箱：**可选高级功能**，默认本机模式（不需要Docker）
- Webhook通知：钉钉/飞书/Slack/通用Webhook
- Appium：v3.2.0 + UiAutomator2驱动（`npm install -g appium && appium driver install uiautomator2`）
- 桌面安装包：`desktop/src-tauri/target/release/bundle/nsis/TestPilot AI_1.0.0_x64-setup.exe`

### v5.0 手机测试进度
- ✅ **v5.0-A** 多平台控制器架构（BaseController/WebController/AndroidController/MobileConfig）
- ✅ **v5.0-B** 13个手机测试API端点 + CLI `mobile` 命令组
- ✅ **v5.0-C** adb截图(秒级) + 移动端AI分析提示词 + 截图分析API
- ✅ **v5.0-D（已完成早期版本）** 前端手机Tab + 设备连接 + 截图预览 + AI分析
- ✅ **v5.0-E（已完成，2026-03-05）** 手机端日志收集 + WebView上下文切换 + 手机蓝本执行器 + MCP工具

### v5.0 手机蓝本测试 —— 今日验证结果（2026-03-05）

**验证环境**
- 设备：vivo V2243A（Android 15，序列号 10AD6Y2XL90030M）
- ADB 连接：正常
- Appium Session：已创建（session_id: mobile_1）
- 截图 API：正常，adb 优先，2.3MB/张

**今日完成流程**
1. `adb devices` → 设备已连接 ✅
2. `poetry run python cli.py mobile devices` → 识别到 1 台设备 ✅  
3. `poetry run python cli.py mobile connect --device 10AD6Y2XL90030M` → Session 创建成功 ✅
4. `/api/v1/mobile/session/mobile_1/screenshot` → 截图正常（2.3MB base64）✅
5. 蓝本测试（BuggyShop，36步）→ **30/36 通过（83%），发现 11 个 Bug，耗时 87 秒** ✅

**v5.0-E 已全部实现（2026-03-05）**：手机端测试闭环已打通，详见下文 6.5 节。

### v5.0 继续开发指南
1. 启动引擎：`poetry run python cli.py serve`（需先 `poetry run python main.py` 确保浏览器 ready）
2. 启动Appium：`appium`（新终端窗口）
3. 插上手机，确认：`python cli.py mobile devices`
4. 连接手机：`python cli.py mobile connect --device <序列号>`
5. 跑蓝本：`python cli.py run -b test-app/testpilot.json`（需先 `Invoke-RestMethod http://localhost:8900/api/v1/browser/launch -Method POST -Body {} -ContentType application/json`）

---

## 六、测试模式使用策略（重要！务必读懂）

### 6.1 通过率的正确理解

**通过率低 ≠ TestPilot 有问题，而是被测应用有 Bug！**

| 通过率 | 含义 |
|--------|------|
| 83%（30/36步通过） | TestPilot 正常工作，在被测应用中发现了 6 处问题 |
| 100% | 应用功能完全符合蓝本预期，没有 Bug |
| 0%（引擎报错） | 说明引擎/浏览器/环境有问题，需要先排查服务 |

**类比**：体检报告里检出 3 个指标异常，不是体检机器坏了——是身体有问题，体检机器工作越精确越好。

### 6.2 蓝本模式（当前主要模式）

**蓝本模式是日常开发的核心，现阶段几乎只用这个。**

特点：
- 按 `testpilot.json` 里的 CSS 选择器精确操作，不靠 AI 猜页面结构
- 速度快：36 步仅 87 秒
- 结果可重复：每次执行路径完全一致
- 适合：开发中功能验证、提交前回归、CI/CD 自动触发

启动流程：
```powershell
# 1. 启动引擎（新窗口）
Start-Process powershell -ArgumentList '-NoExit','-Command','cd D:\Projects\TestPilotAI; poetry run python main.py'

# 2. 启动浏览器（一次性，重启引擎后需重新执行）
Invoke-RestMethod http://localhost:8900/api/v1/browser/launch -Method POST -Body '{}' -ContentType 'application/json'

# 3. 启动被测应用（例如 BuggyShop）
Start-Process powershell -ArgumentList '-NoExit','-Command','cd D:\Projects\TestPilotAI\test-app; python -m http.server 8080'

# 4. 跑蓝本测试
poetry run python cli.py run -b test-app\testpilot.json -f json -o data\report.json
```

### 6.3 探索模式（暂缓使用）

**探索模式目前不是优先项，有以下限制：**
- AI 自由操作页面，每步都要截图+AI 分析，速度慢（分钟级）
- 结果不确定性高，难以回归对比
- **适合场景**：夜间/闲时让它在后台跑，发现蓝本之外的意外 Bug
- **目前建议**：优先把蓝本写全，覆盖所有核心场景。探索模式留到后续版本或夜间挂机用

### 6.4 真机蓝本测试——屏幕尺寸与选择器

**核心结论：蓝本模式天生支持多机型，不存在屏幕尺寸问题。**

| 测试类型 | 蓝本写法 | 与屏幕尺寸关系 |
|---------|---------|-------------|
| 手机浏览器打开网页 | CSS 选择器（`#addBtn`、`.submit-btn`） | **无关**，浏览器负责布局，选择器永远精确 |
| 原生 App（APK） | resource-id（`com.app:id/btn_submit`）或 accessibility-id | **无关**，元素 ID 不随屏幕变化 |
| 滑动（swipe）操作 | 使用百分比坐标（如"从屏幕 50%位置滑到 20%"）| 需要做适配，用百分比而非固定像素 |

**结论**：同一份蓝本在 5 寸小屏手机和 7 寸平板上跑，结果完全一致，不需要为不同设备写多份蓝本。

**v5.0-E 已完成（2026-03-05）**：新增独立 `POST /api/v1/test/mobile-blueprint` 端点（传 `mobile_session_id`），由 `MobileBlueprintRunner` 通过 Appium + WebView 上下文执行蓝本，同一份 `testpilot.json` 真机可直接运行。

### 6.5 手机蓝本格式 vs 日志收集——详细对比

#### 蓝本格式（testpilot.json）

| 场景 | 选择器格式 | 是否需要新字段 |
|------|-----------|--------------|
| 手机浏览器测 Web 网页 | CSS 选择器（`#addBtn`、`.btn`）**完全相同** | 不需要，现有格式直接复用 |
| 原生 App（APK） | `resource-id`（`com.app:id/submit`）或 `accessibility-id` | 需要加 `"platform": "android"` 字段区分选择器类型 |

**重要**：当前阶段做的是"手机浏览器打开网页"，蓝本格式与桌面 Web 测试 **100% 一致**，同一份 `testpilot.json` 两端通用。

原生 APK 蓝本示例（未来扩展）：
```json
{
  "platform": "android",
  "app_package": "com.example.myapp",
  "scenarios": [{
    "steps": [
      { "action": "tap", "target": "id:com.example.myapp:id/btn_login", "description": "点击登录" },
      { "action": "input", "target": "id:com.example.myapp:id/et_username", "value": "admin" },
      { "action": "screenshot", "expected": "登录成功，跳转到首页" }
    ]
  }]
}
```

#### 日志收集对比

| 类型 | 桌面 Web（Playwright） | 手机 Web（Appium + adb） | 状态 |
|------|----------------------|------------------------|------|
| JS 控制台错误 | `page.on("console")` — 实时精确 | `AndroidController.get_browser_log()` → Appium `POST /log {"type":"browser"}` | ✅ **v5.0-E 已实现** |
| App 系统日志 | 不适用 | `AndroidController.start_logcat()` → `adb logcat -v threadtime`，守护线程持续收集，步骤失败时自动注入最近 30 行 | ✅ **v5.0-E 已实现** |
| 网络请求错误 | `page.on("response")` — 精确拦截 | adb 抓包或手机浏览器 DevTools（需要额外工具） | ⏳ 暂不支持 |
| 布局溢出检测 | Playwright `page.evaluate()` 运行 JS 检查 DOM | 截图后 AI 判断（没有直接访问 DOM 的手段） | ⚠️ 改为截图分析 |
| App 崩溃/ANR | 不适用 | logcat 自动包含 FATAL/ANR，`get_logcat()` 可过滤 | ✅ **v5.0-E 已实现** |

**v5.0-E 已实现（2026-03-05）**：
- ✅ `AndroidController.start_logcat()` — launch() 时自动启动，守护线程收集，缓冲最近 5000 行
- ✅ `AndroidController.get_logcat(last_n)` — 步骤失败/Error 时自动注入最近 30 行到 Bug 报告
- ✅ `AndroidController.get_browser_log()` — Appium `POST /log {"type":"browser"}` 获取 JS 控制台日志
- ✅ `AndroidController.switch_to_webview()` — 轮询等待 WebView 上下文出现后切换（支持 CSS 选择器操作网页元素）
- ✅ `MobileBlueprintRunner`（新文件 `src/testing/mobile_blueprint_runner.py`）— 真机蓝本执行器
- ✅ `POST /api/v1/test/mobile-blueprint` — 新 API 端点，传 `mobile_session_id` 即可
- ✅ MCP 工具 `run_mobile_blueprint_test()` — Cursor/Windsurf 可直接调用，支持自动创建 Session

---

## 七、MCP 配置（编程AI双向通信）

### 什么是 MCP
MCP（Model Context Protocol）让编程AI（Windsurf/Cursor）直接调用 TestPilot 的测试能力。
实现闭环：编程AI写代码 → TestPilot测试 → 报Bug → 编程AI修复 → 再测 → 直到0Bug。

### Windsurf 配置
MCP配置文件位置：`~/.codeium/windsurf/mcp_config.json`（即 `C:\Users\用户名\.codeium\windsurf\mcp_config.json`）

在 `mcpServers` 对象里添加以下配置（和其他MCP并列）：

```json
"testpilot": {
  "command": "D:\\projects\\TestPilotAI\\.venv\\Scripts\\python.exe",
  "args": ["D:\\projects\\TestPilotAI\\run_mcp.py"],
  "env": {}
}
```

**⚠️ 注意：必须用 `run_mcp.py` 绝对路径方式，不能用 `-m src.mcp_server`（会有 PYTHONPATH 问题）**

### 新电脑安装 MCP 步骤（完整）
1. 先完成项目初始化（poetry install + playwright install）
2. 用 Windsurf 打开 `~/.codeium/windsurf/mcp_config.json`
3. 在 `mcpServers` 里添加上面的 `testpilot` 配置
4. **修改 command 和 args 里的路径为实际项目路径**
5. 重启 Windsurf（或 `Ctrl+Shift+P` → `Reload Window`）
6. 启动引擎：`poetry run python main.py`
7. 在 Windsurf 聊天里验证：让AI调用 `check_engine_health` 工具

### MCP 工具列表（11个）
| 工具 | 功能 |
|------|------|
| `run_blueprint_test` | 按蓝本测试，返回Bug列表 |
| `run_quick_test` | 无蓝本快速测试（盲测） |
| `get_test_report` | 获取最近测试报告 |
| `check_engine_health` | 检查引擎状态 |
| `generate_blueprint_template` | 生成蓝本模板 |
| `generate_blueprint` | 从URL自动生成蓝本（v10.1） |
| `list_blueprints` | 扫描工作区蓝本列表（v10.2） |
| `run_blueprint_batch` | 批量执行多个蓝本（v10.2） |
| `run_mobile_blueprint_test` | 真机蓝本测试（v5.0-E） |
| `run_desktop_test` | 桌面应用测试（v7.0） |
| `run_multiplayer_test` | 多端协同测试（v9.0） |

### 重要提醒
- **MCP配置修改后必须重启Windsurf才生效**
- **测试前必须先启动引擎**：`poetry run python main.py`
- **Windows下 TP_SERVER_RELOAD 必须设为 false**（否则Playwright子进程报错）
- **可视化测试**：`.env` 中 `TP_BROWSER_HEADLESS=false` 可看到浏览器自动操作

---

## 七、MCP 踩坑记录（重要！）

开发 MCP Server 过程中遇到的坑，单位电脑安装时注意：

### 坑1：MCP配置文件位置
- ❌ 错误：`~/.windsurf/mcp_config.json`
- ✅ 正确：`~/.codeium/windsurf/mcp_config.json`（即 `C:\Users\用户名\.codeium\windsurf\mcp_config.json`）

### 坑2：模块导入路径
- ❌ 错误配置：`"args": ["-m", "src.mcp_server"]` — MCP进程没有正确的工作目录，会报 ModuleNotFoundError
- ✅ 正确配置：`"args": ["D:\\projects\\TestPilotAI\\run_mcp.py"]` — 用 `run_mcp.py` 入口脚本，里面会自动设置 sys.path

### 坑3：httpx 在 MCP 进程中网络异常
- ❌ 用 `httpx`（同步或异步）都会报 `[WinError 10054] 远程主机强迫关闭连接`
- ✅ 用 Python 标准库 `urllib.request` 替代，零依赖且稳定

### 坑4：PowerShell 的 ConvertTo-Json 破坏格式
- ❌ 用 PowerShell 修改 JSON 会搞乱缩进和字段顺序
- ✅ 用 Python 脚本修改 JSON，或者直接在 Windsurf 里手动编辑

### 坑5：每次修改 MCP Server 代码需要 Reload
- MCP Server 是 Windsurf 启动时加载的独立进程
- 修改 `src/mcp_server.py` 或 `run_mcp.py` 后，必须 `Ctrl+Shift+P` → `Reload Window`
- Reload 会杀掉所有后台进程（引擎也会停），需要重新启动引擎

### 坑6：JSON 文件 BOM 编码
- PowerShell 写入的 JSON 可能带 UTF-8 BOM 头
- Python 读取时需要用 `encoding='utf-8-sig'` 而不是 `'utf-8'`

---

## 八、蓝本（testpilot.json）格式完整说明

蓝本是编程AI写完功能后生成的测试规范，TestPilot 按蓝本精确执行测试。

### 完整格式

```json
{
  "app_name": "应用名称",
  "base_url": "http://localhost:3000",
  "version": "1.0",
  "pages": [
    {
      "url": "/",
      "title": "页面标题",
      "elements": {
        "输入框": "#todoInput",
        "添加按钮": "#addBtn",
        "列表": ".todo-list"
      },
      "scenarios": [
        {
          "name": "场景名称",
          "description": "场景描述",
          "steps": [
            {
              "action": "navigate",
              "value": "http://localhost:3000",
              "expected": "页面正常加载",
              "description": "打开首页"
            },
            {
              "action": "fill",
              "target": "#todoInput",
              "value": "要输入的文字",
              "description": "输入待办内容"
            },
            {
              "action": "click",
              "target": "#addBtn",
              "description": "点击添加按钮"
            },
            {
              "action": "assert_visible",
              "target": ".todo-item",
              "description": "验证待办项出现"
            },
            {
              "action": "screenshot",
              "expected": "列表中显示刚添加的待办，统计栏数字正确",
              "description": "截图验证状态"
            }
          ]
        }
      ]
    }
  ]
}
```

### 支持的 action 类型

| action | 说明 | 必须字段 |
|--------|------|----------|
| `navigate` | 打开URL | `value`（URL） |
| `click` | 点击元素 | `target`（CSS选择器） |
| `fill` | 输入文字 | `target` + `value` |
| `select` | 下拉选择 | `target` + `value` |
| `wait` | 等待元素出现 | `target` + 可选 `timeout_ms` |
| `screenshot` | 截图+AI验证 | `expected`（预期描述） |
| `assert_visible` | 验证元素可见 | `target` |
| `assert_text` | 验证文本内容 | `target` + `value`（预期文本） |
| `scroll` | 滚动页面 | — |

### v1.3 新增字段
| 字段 | 说明 | 示例 |
|------|------|------|
| `wait_after_ms` | 操作后等待毫秒（处理异步加载/动画） | `"wait_after_ms": 500` |
| `auto:` 前缀 | value字段智能生成测试数据 | `"value": "auto:number:1-999"` |

**auto: 智能输入支持的类型**：
- `auto:number:1-999` — 随机整数
- `auto:number:0.01-99.99` — 随机浮点数
- `auto:text:人名` / `auto:text:公司名` / `auto:text:地址` / `auto:text:句子`
- `auto:email` — 随机邮箱
- `auto:phone` — 随机手机号
- `auto:date` — 随机日期
- `auto:password` — 随机密码（含大小写+数字）

### 关键要点
1. **`target` 必须是精确的CSS选择器**：`#id`、`.class`、`[data-xxx]`、`.parent .child` 等
2. **`expected` 写自然语言**：AI会看截图判断是否符合预期，写越详细越好
3. **一个场景失败不影响后续场景**：TestPilot会继续测完所有场景
4. **Demo蓝本参考**：`demo-app/testpilot.json`（44步，10个场景，覆盖Todo+购物清单）

---

## 九、重要经验与踩坑

### 环境相关
- **Windows + Uvicorn reload 不兼容 Playwright**：必须 `TP_SERVER_RELOAD=false`
- **pydantic-settings 子配置必须加 env_file**：否则读不到 .env 变量
- **端口统一8900**：前端 vite.config.ts / engineClient.ts / AppLayout.tsx 全部指向 8900

### Playwright 相关
- **Playwright缓存问题**：复用browser context会缓存旧HTML。解决：`Network.clearBrowserCache`（CDP）
- **analyze_screenshot 是同步方法**：不能用 await 调用
- **探索模式 vs 蓝本模式**：探索模式AI猜选择器准确率低（25%）；蓝本模式精确CSS选择器，98%通过率

### Tauri 相关
- **Windows Defender文件锁**：编译时 `.o` 文件被杀毒软件锁定。解决：排除 `src-tauri/target` 目录
- **`CARGO_INCREMENTAL=0`**：禁用增量编译可绕过部分文件锁问题
- **tauri.conf.json**：Tauri 2.x 的 `app` 节点下不能直接放 `title`，只能在 `windows[]` 里

### AI模型相关
- **Doubao-Seed-1.8 价格**：输入0.8/输出2.0元/百万token（含视觉），典型20步测试成本0.017元
- **记忆系统**：Bug模式从结构化字段提取（零AI成本），AI压缩仅在同URL测试≥3次时触发

### 日志智能切片（v2.1新增）
- **按步骤时间戳切片**：`LogSlicer` 在步骤start/end间收割浏览器控制台+网络错误
- **只喂失败步骤**：passed步骤日志直接丢弃，每步最多50行，error优先
- **三种日志源**：终端日志（process_runner） + 浏览器控制台（page.on('console')） + 网络错误（page.on('response') 4xx/5xx）
- **实战验证**：蓝本测试44步，Bug报告自动包含"JS控制台错误: Failed to load resource"

### v5.1 手机测试增强（2026-03-05）
- **蓝本权限字段**：Blueprint新增`platform`/`permissions`/`app_package`/`app_activity`，API session创建时自动`adb pm grant`批量授权，短名自动补全`android.permission.`前缀
- **厂商弹窗选择器库**：`VendorDialogRegistry`内置18条规则覆盖AOSP/华为/小米/OPPO/vivo/三星，按priority排序；`DialogDismisser`后台2s轮询检测+点击dismiss；`detect_vendor()`根据manufacturer自动匹配厂商
- **Docker Android模拟器**：`docker-compose.android.yml`使用`budtmo/docker-android:emulator_13.0`镜像，noVNC(6080)+Appium(4723)+ADB(5554/5555)；`EmulatorManager`支持`async with`自动启停+健康检查
- **ConsoleCollector**：统一收集console输出(log/warn/error)+HTTP错误(4xx/5xx)+请求失败(CORS/超时/DNS/连接)+JS异常；自动绑定到`BrowserAutomator.launch()`；`_classify_failure()`归类网络错误；`summary()`生成摘要含`has_critical_errors`标志
- **测试新增94个**：test_blueprint(+5) + test_android_controller(13) + test_vendor_dialogs(22) + test_emulator_manager(18) + test_console_collector(36)，总计488

### v5.2 报告分析可视化（2026-03-05）
- **ReportAnalytics引擎**：`src/testing/report_analytics.py`，从test_history表聚合数据，5个核心方法：`get_pass_rate_trend`（折线图）、`get_screenshot_timeline`（步骤回放）、`get_bug_heatmap`（四维统计）、`compare_reports`（Bug diff）、`export_html_report`（独立HTML）
- **5个新API端点**：`/analytics/trend`、`/analytics/timeline/{id}`、`/analytics/heatmap`、`/analytics/compare`、`/analytics/export/{id}`（返回HTMLResponse）
- **前端AnalyticsPage**：三Tab布局（趋势/热力图/对比），纯CSS柱状图+条形图，无额外依赖；历史对比支持选择任意两次测试；HTML报告一键导出
- **Bug热力图四维度**：by_page（哪个URL bug最多）、by_category（UI/CSS/逻辑分类）、by_severity（critical/major/minor占比条形图）、by_location（高频问题元素排行）
- **测试新增20个**：趋势(5) + 时间线(3) + 热力图(5) + 对比(3) + HTML导出(4)，总计508

### v6.0 用户系统+商业化基础（2026-03-05）
- **SQLAlchemy双驱动**：`src/auth/database.py`，默认SQLite（`data/testpilot.db`），环境变量`DATABASE_URL`切换PostgreSQL；SQLite自动WAL+外键；PG连接池size=10/overflow=20
- **三表模型**：`src/auth/models.py`，User（邮箱+密码哈希+角色+配额字段）、Project（名称+URL+所有权+统计缓存）、UsageRecord（按日聚合+唯一索引user_id+date）
- **角色与配额**：free(10测试/天,3项目,50AI调用) / pro(100/20/500) / admin(无限)；`ROLE_QUOTAS`字典驱动，升级只需改配额
- **认证服务**：`src/auth/service.py`，bcrypt直接调用（兼容bcrypt 5.x，72字节截断）；JWT HS256，24h有效期，环境变量`JWT_SECRET_KEY`覆盖
- **鉴权中间件**：`src/auth/middleware.py`，`get_current_user`（必须登录）、`get_current_user_optional`（游客兼容）、`require_role(*roles)`工厂函数
- **12个API端点**：`src/auth/routes.py`，注册/登录/个人信息 + 项目CRUD(5个) + 用量查询/配额检查；集成到`src/app.py`的`/api/v1`前缀下
- **前端DashboardPage**：登录/注册切换表单；已登录展示用量统计卡片(4个)+项目列表(创建/删除)；token存localStorage自动附加到`authRequest`
- **passlib弃用**：passlib与bcrypt 5.x不兼容（`detect_wrap_bug`内部用>72字节密码触发ValueError），改为直接用`bcrypt.hashpw/checkpw`
- **测试新增26个**：密码(2) + JWT(3) + 注册登录(6) + 项目CRUD(6) + 用量(4) + 模型(3) + 非法场景(2)，总计534

### v6.1 团队协作（2026-03-05）
- **Team/TeamMember模型**：`src/auth/models.py`新增两表，Team（name+owner+invite_code+max_members）、TeamMember（team_id+user_id+role唯一索引）；Project新增`team_id`可选外键
- **三级角色**：`TEAM_ROLE_ADMIN`/`TEAM_ROLE_TESTER`/`TEAM_ROLE_VIEWER`，admin可管理成员+变更角色+重生成邀请码，仅owner可删除团队
- **团队服务**：`src/auth/team_service.py`，13个函数：`create_team`/`join_team`/`remove_member`/`change_role`/`share_project`/`unshare_project`/`get_team_projects`/`get_team_dashboard`/`regenerate_invite`/`delete_team`等
- **11个API端点**：`src/auth/team_routes.py`，POST /teams、GET /teams、POST /teams/join、GET/POST members、GET projects、POST share/unshare、GET dashboard、POST regenerate-invite、DELETE team
- **前端TeamPage**：团队列表卡片+创建/邀请码加入+团队看板（统计卡片+成员列表+共享项目）；未登录时显示提示
- **测试新增27个**：创建(3) + 加入(4) + 成员管理(4) + 角色(3) + 项目共享(5) + 看板(2) + 邀请码+删除(4) + 模型(2)，总计561

### v7.0 桌面应用测试（2026-03-05）
- **DesktopController**：`src/controller/desktop.py`，继承`BaseController`，通过ctypes调用Win32 API（SendInput鼠标点击+Unicode键盘输入），通过PowerShell调用.NET UI Automation查找控件
- **WindowManager**：`src/controller/window_manager.py`，静态方法：`enumerate_windows`/`find_window`/`focus_window`/`resize_window`/`get_window_rect`/`capture_window`/`capture_screen`；纯BMP文件写入（struct.pack），零PIL依赖
- **四种选择器**：`name:XX`（按Name属性）、`automationid:XX`（按AutomationId）、`class:XX`（按ClassName）、`point:X,Y`（直接坐标）；PowerShell脚本动态构造PropertyCondition查询
- **窗口截图**：`PrintWindow` API + `GetDIBits` 读取像素 → BMP文件；全屏截图用`BitBlt`从桌面DC拷贝
- **11个API端点**：`src/api/routes.py`新增桌面测试区段，GET /desktop/windows枚举、POST session/create连接、POST tap/input操作、GET screenshot/source/text查询、POST navigate切换、DELETE session关闭、POST analyze AI分析
- **前端DesktopTestPage**：窗口列表选择连接 → 操作面板（选择器输入+6个操作按钮）→ 截图预览+UI树双栏 → 操作日志；侧栏新增「桌面测试」入口
- **测试新增32个**：WindowInfo(3) + DesktopConfig(2) + WindowManager(6) + DesktopController(12) + PS查找(4) + BMP写入(1) + 完整mock Win32 API，总计593

### v8.0 微信小程序测试（2026-03-05）
- **MiniProgramController**：`src/controller/miniprogram.py`，继承`BaseController`，通过Node.js子进程调用`miniprogram-automator` SDK与微信开发者工具通信
- **Node.js桥接脚本**：`src/controller/miniprogram_bridge.js`，接收JSON命令→调用automator API→输出JSON结果；支持connect/disconnect/navigateTo/tap/input/screenshot/getWxml/getText/elementExists/getCurrentPage/callWxApi/mockWxApi/getAppData/getPageData共14个action
- **长连接桥接服务器**：`src/controller/miniprogram_bridge_server.js`（HTTP端口9421），Python通过HTTP请求通信，全部操作1.7秒完成（旧模式每次重连60秒超时）
- **MiniProgramConfig**：自动检测开发者工具路径（4个常见位置），可配置project_path/devtools_path/timeout_ms/account
- **选择器格式**：CSS选择器（`.class`/`#id`/`view`/`.parent .child`），与小程序automator SDK一致
- **特有功能**：`get_page_data()`获取页面data、`get_app_data()`获取globalData、`call_wx_api()`调用微信API、`mock_wx_api()`模拟微信API返回（可模拟定位/支付等）、`wait_for_element()`等待元素出现
- **11个API端点**：`src/api/routes.py`新增小程序测试区段，GET devtools/status检测、POST session/create连接、POST navigate/tap/input操作、GET screenshot/source/text/page-data查询、DELETE session关闭、GET sessions列表
- **前端MiniProgramTestPage**：开发者工具状态检测 → 项目路径输入+连接 → 操作面板（导航/点击/输入/截图/WXML/页面数据）→ 截图预览+数据双栏 → 操作日志；侧栏新增「小程序测试」入口
- **测试新增34个**：Config(4) + Controller基础(3) + launch/close(4) + navigate(2) + tap(2) + input(2) + screenshot(2) + source/text(3) + page/app data(3) + wx api(2) + bridge调用(5) + wait_for(2)，总计627

### v9.0 多端协同测试 Phase1（2026-03-05）
- **MultiPlayerOrchestrator**：`src/testing/multiplayer.py`，核心协调器，管理N个玩家(最多8人)的生命周期、操作执行、并行调度、时序轴记录
- **PlayerSlot**：玩家槽位，封装controller实例+状态(idle/connecting/ready/executing/waiting/done/error/disconnected)+截图列表+日志
- **SyncBarrier**：同步屏障，指定玩家全部arrive()后才放行，支持超时
- **EventBus**：全局事件总线，on/off/emit，支持同步和异步监听器，自动记录事件历史
- **MultiPlayerBlueprint**：`src/testing/multiplayer_blueprint.py`，多人蓝本解析与执行器，支持6种步骤类型：player action（单人操作）、sync（同步屏障）、parallel（并行执行）、wait_for（条件等待+screen_contains）、assert（断言）、assert_consistency（跨端一致性截图对比）
- **蓝本格式**：`{"mode":"multiplayer","players":[...],"steps":[...]}`，players支持web/android/desktop/miniprogram混合，steps支持嵌套parallel数组
- **12个API端点**：`src/api/routes.py`新增多人测试区段，POST room/create创建房间、POST room/player添加玩家、DELETE room/player移除、POST player/action执行操作、POST room/parallel并行执行、POST room/sync同步屏障、POST room/screenshot-all全员截图、GET room/status房间状态、GET room/timeline时序轴、POST room/start开始、POST room/stop停止、POST blueprint/run蓝本执行、DELETE room销毁
- **前端MultiPlayerTestPage**：控制面板（新建房间/添加玩家/开始/停止/全员截图/销毁）→ 玩家网格视图(2×2自适应+状态+截图计数) → 时序轴视图(offset/player/action/duration/success) → 操作日志；侧栏新增「多人测试」入口
- **测试新增59个**：PlayerSlot(3) + TimelineEvent(1) + SyncBarrier(5) + EventBus(7) + Orchestrator(22) + Blueprint(14) + 蓝本执行(7)，总计686

### v9.0 多端协同测试 Phase2（2026-03-05）
- **AIPlayerEngine**：`src/testing/ai_player.py`，AI自动扮演引擎，4种策略(random/normal/boundary/explorer)，分析页面源码自动决策下一步操作
- **AIStrategy**：随机(random)点击可交互元素、正常(normal)优先按钮>输入>链接、边界(boundary)输入极端值/XSS/SQL注入、探索(explorer)优先未访问元素
- **AIAction**：决策数据结构，包含action/params/reason/confidence，记录AI为什么做出该决策
- **ActionRecorder**：`src/testing/action_recorder.py`，操作录制器，实时录制多端操作序列，支持start/stop/record，自动计算时间偏移offset
- **ActionReplayer**：操作回放器，支持倍速回放(speed参数)、按玩家过滤回放(player_filter)、可中断sleep、进度追踪
- **录制→蓝本导出**：`export_blueprint()`将录制操作导出为标准多端蓝本JSON格式，`save()`保存到文件，`load_recording()`从文件加载
- **ConsistencyChecker**：`src/testing/consistency_checker.py`，跨端一致性验证器，所有端同时截图→MD5哈希对比→文本相似度→差异报告→一致性得分(0-100)
- **一致性报告**：ConsistencyReport包含consistent布尔值、score得分、diffs差异列表(field/severity/player_a/player_b)，支持to_dict()序列化
- **12个新API端点**：AI扮演(POST ai/start启动+POST ai/stop停止+GET ai/report报告)、录制(POST record/start+stop+action+GET export)、回放(POST replay/start+stop+GET status)、一致性(POST consistency/check+GET summary)
- **Bug修复**：AI玩家error分支未递增action_count导致无限循环，已修复
- **测试新增52个**：AIModels(3) + AIPlayerEngine(13) + ActionRecorder(8) + ActionReplayer(7) + ConsistencyModels(4) + ConsistencyChecker(12) + 边界(5)，总计738

### v9.0 多端协同测试 Phase3（2026-03-05）
- **NetworkSimulator**：`src/testing/network_simulator.py`，网络模拟器，7种预设环境(perfect/wifi/4g/3g/slow/unstable/offline)，支持全局和按端设置
- **NetworkConfig**：可配置latency_ms(延迟)+jitter_ms(抖动)+packet_loss(丢包率)+bandwidth_kbps(带宽限制)，从预设加载或自定义
- **模拟逻辑**：simulate()先判丢包(随机数<packet_loss则丢弃)，再注入延迟(base±jitter)ms，记录事件日志
- **DevicePoolManager**：`src/testing/device_pool.py`，设备池管理器，最多20台设备，5种设备类型(browser/android/ios/desktop/miniprogram)
- **DeviceInfo**：设备ID+类型+名称+状态(available/in_use/offline/maintenance)+capabilities+分配玩家+心跳+tags
- **设备管理**：register/unregister注册注销、acquire/release分配释放、auto_assign批量分配、heartbeat心跳、check_health健康检查(超时标记offline)、release_all全释放
- **13个新API端点**：网络模拟(POST enable+disable+profile+custom, GET stats) + 设备池(POST register+acquire+release+auto-assign+release-all+health-check, DELETE device, GET summary)
- **前端增强**：多端协同页新增5个视图页签(网格/时序轴/AI扮演/录制回放/一致性)，AI视图(策略选择+每端启停+报告)，录制视图(开始/停止/导出+倍速回放+进度条)，一致性视图(执行检查+得分+差异详情+历史摘要)
- **测试新增48个**：NetworkModels(7) + NetworkSimulator(11) + DeviceModels(3) + DevicePoolManager(27)，总计786

---

## 2026-03-05 下午 — 端到端验收 + MCP闭环 + VSCode插件

### 完成项目

1. **端到端验收全部通过**
   - 引擎启动 8900端口 ✅
   - 前端11页侧栏全部渲染 ✅
   - 用户系统API全部通过（注册→登录→项目→团队→共享→看板）✅
   - 蓝本测试BuggyShop 5个Bug全部找到（10-11条Bug报告/36步/83%通过率）✅

2. **MCP闭环验证通过**
   - `check_engine_health` ✅ → `run_blueprint_test` ✅
   - 链路：Windsurf → MCP → TestPilot引擎 → 浏览器自动化 → AI视觉分析 → Bug报告返回
   - 配置路径：`C:\Users\37561\.codeium\windsurf\mcp_config.json`

3. **报告日志递交修复**
   - `src/api/models.py`：新增 `StepDetail` + `BugDetail` 模型
   - `src/api/routes.py`：蓝本测试API返回完整steps和bugs（含日志切片）

4. **VSCode/Windsurf 插件 v1.0 安装**
   - 编译+打包：`extension/testpilot-ai-1.0.0.vsix`（82KB）
   - 侧边栏面板：引擎状态、蓝本/探索双模式、实时日志、测试结果
   - 插件打磨：Bug列表（严重度标签）、步骤详情折叠、🔄重测按钮、🤖发送给AI修复按钮
   - pass_rate修复：API返回百分比83，不是小数0.83
   - `engineClient.ts`：添加 StepDetail/BugDetail 接口

5. **Rust环境迁移到D盘**
   - `RUSTUP_HOME` = `D:\DevEnv\.rustup`
   - `CARGO_HOME` = `D:\DevEnv\.cargo`
   - rustc 1.93.1 + cargo 1.93.1 + cargo-tauri 2.10.1
   - 环境变量已永久写入用户级别

### 已知问题
- `windsurf.newCascade` 命令不存在，"发送给AI修复"走剪贴板兜底
- 每次 Reload Window 需要手动重启引擎

### v10.0 晚间更新（2026-03-05 晚 家中）

1. **插件侧边栏修复**
   - 问题：`--no-dependencies`打包导致`ws`模块缺失，扩展激活崩溃
   - 修复：正常打包包含`node_modules/ws`，`.vsix`从42KB→84KB
   - 安装：`windsurf --install-extension testpilot-ai-1.0.0.vsix --force`

2. **英文命令支持**
   - 每个中文命令都有对应英文版，命令面板搜索 `TestPilot AI` 可见全部
   - 英文→中文映射通过 `vscode.commands.executeCommand` 转发
   - 中英文命令对照：Start Test / Blueprint Test / Stop/Pause/Resume Test / View Report / Auto Fix Bugs / Check Engine

3. **一键启动（CLI增强）**
   - `python cli.py serve` 现在自动检查：端口占用 → Playwright浏览器 → AI密钥 → 仪表盘
   - 新增参数：`--force`（端口占用时强制启动）、`--open`（自动打开浏览器）
   - Playwright浏览器未安装时自动安装

4. **关于闭环测试的说明**
   - 插件不能直接通过API把Bug发给编程AI（没有这种API）
   - 闭环通过MCP实现：在Cascade聊天中说"帮我修复"，MCP工具自动执行 测试→修复→重测
   - 已有MCP工具：`run_blueprint_test`、`check_engine_health`等
   - 闭环链路：用户说话 → Cascade调MCP → 引擎测试 → 返回Bug → Cascade改代码 → 再调MCP重测

5. **桌面应用编译**（已完成✅）
   - Tauri v2 配置：`desktop/src-tauri/tauri.conf.json`
   - 构建产物：EXE + MSI + NSIS 三个安装包
   - 前端：React + TailwindCSS + Vite（端口5173，代理到引擎8900）

6. **测试覆盖率提升**（已完成✅）
   - 新增7个测试文件、127个测试用例
   - 总测试数：786 → 913，全部通过
   - 覆盖模块：action_recorder、consistency_checker、network_simulator、device_pool、mcp_server、auth_service、webhook

7. **闭环修复演示**（已完成✅）
   - 用 buggy_shop 测试商城进行了完整闭环：测试→发现7个Bug→修复→重测→100%通过
   - 修复：价格不一致、空密码登录、浮点精度、未登录结算、大金额报错、页面布局
   - 闭环经历4轮：79% → 88% → 89% → 100%

8. **sqlalchemy依赖问题**（已修复✅）
   - 现象：`poetry run python cli.py serve --force` 报 `ModuleNotFoundError: No module named 'sqlalchemy'`
   - 原因：虚拟环境未正确激活，必须用 `poetry run` 前缀运行
   - 解决：确保始终使用 `poetry run python cli.py serve` 启动

### 需要带去单位的安装包
1. **Windsurf/VSCode插件**：`extension/testpilot-ai-1.0.0.vsix`
   - 安装：`windsurf --install-extension testpilot-ai-1.0.0.vsix --force`
2. **桌面应用安装包**（在 `desktop/src-tauri/target/release/bundle/` 下）：
   - `nsis/TestPilot AI_0.1.0_x64-setup.exe`（推荐，NSIS安装包）
   - `msi/TestPilot AI_0.1.0_x64_en-US.msi`（MSI安装包）

### v10.1 完成记录（2026-03-06）

#### 核心变更

**引擎内置静态预览服务器**
- `src/app.py`：启动时自动扫描工作区子目录，凡含 `testpilot.json` 的目录自动挂载到 `/preview/{dirname}/`
- 访问：`http://localhost:8900/preview/shop-demo/`、`/preview/test-app/`、`/preview/demo-app/`
- 含路径穿越安全检查（resolve + relative_to 验证）
- 一键启动引擎 = 被测应用同时可访问，不再需要手动 `python -m http.server`

**蓝本自动生成**
- `src/testing/blueprint_generator.py`：`BlueprintGenerator` 类
  - `from_url(url)`：Playwright打开→JS提取可交互元素→截图→清理HTML→AI多模态分析→蓝本
  - `from_html(html)`：纯文本模式，不启动浏览器
  - `_ai_generate()`：有截图走 `analyze_screenshot()`，无截图走 `chat()`，都用 `asyncio.to_thread` 异步包装
- `POST /api/v1/blueprint/generate`：API端点，接收 url/app_name/description/output_path
- `GET /api/v1/preview/apps`：列出所有可预览应用

**自动 URL 切换**
- `_auto_preview_url()` 函数（`src/api/routes.py`）：蓝本测试时检测 base_url 端口是否可达（0.5s超时），不可达且蓝本在工作区内、含 index.html 时，自动切换为 `/preview/{dirname}/`

**MCP 工具**
- `src/mcp_server.py` 新增 `generate_blueprint` 工具（共7个工具）
- Cursor/Windsurf 可直接说"帮我生成蓝本 `http://localhost:3000`"

**插件**
- `engineClient.ts` 新增 `generateBlueprint()` 方法
- `sidebarProvider.ts` 蓝本面板新增"自动生成蓝本"区域（URL+名称+描述输入+按钮）
- 生成成功后自动填入蓝本路径并触发重新扫描

**测试**
- `tests/test_blueprint_generator.py`：19个测试，全部通过
- 总测试数：913 → 932

**vsix**
- 重新打包：`extension/testpilot-ai-1.0.0.vsix`（183KB）
- 安装命令：`windsurf --install-extension extension/testpilot-ai-1.0.0.vsix --force`

**demo蓝本已更新**
- `shop-demo/testpilot.json`：`base_url` → `http://localhost:8900/preview/shop-demo/`
- `test-app/testpilot.json`：`base_url` → `http://localhost:8900/preview/test-app/`
- `demo-app/testpilot.json`：`base_url` → `http://localhost:8900/preview/demo-app/`

---

### 下次待办（v10.2，2026-03-06更新）

#### 已完成 ✅
- MCP in VS Code（`.vscode/mcp.json`）配置完成，Copilot Chat可调用5个工具
- shop-demo 完整闭环验证：10场景79步，4个预埋Bug全部发现，修复后100%通过
- 蓝本生成提示词强化：16条硬规则+自检清单
- **v10.1 蓝本自动生成全部完成**（蓝本生成器+Preview服务器+MCP+插件+测试）

#### 下一步优先级（v10.2）

1. **多端Demo验证**：
   - 手机：shop-demo加响应式布局 + USB连Android手机 + `run_mobile_blueprint_test`
   - 小程序：安装微信开发者工具 + miniprogram-automator + 新建含Bug小程序
   - 桌面：用Tauri桌面应用（已安装）为被测对象，`DesktopController` 跑测试

2. **蓝本功能描述模式（v10.3）** — 终极目标：
   - URL + "这个应用有：登录、商品管理、订单管理" → AI全自动蓝本 → 测试 → 修复 → 0 Bug
   - **无需录制，无需人工参与，完全自动化**

#### 关于"Bug列表不更新"的说明
- 插件侧边栏的Bug列表 = **上一次通过插件UI触发的测试结果**
- 通过MCP（Copilot Chat命令）跑的测试，结果只显示在Chat里，不回写插件UI

---

### v10.2 完成记录（2026-03-07）

#### 核心功能

**独立蓝本多选+批量执行**
- Blueprint模型添加 `description` 字段（50-200字功能说明）
- 后端API：`GET /blueprint/list` 扫描蓝本列表 + `POST /test/blueprint-batch` 批量执行
- MCP工具：`list_blueprints` + `run_blueprint_batch`（共11个工具）
- 插件UI：下拉框改为多选框列表，显示蓝本元数据+悬浮说明预览
- 全选按钮 + 批量测试汇总报告

**蓝本统一存放规范**
- 6个demo蓝本全部移入 `testpilot/` 子文件夹
- 预览服务器扫描逻辑适配 `testpilot/` 目录
- copilot-instructions更新：要求按功能模块拆分蓝本（auth/product/order）

**start_command自动启动应用**
- Blueprint模型添加 `start_command` 和 `start_cwd` 字段
- BlueprintRunner测试前自动启动应用并等待就绪（最多30秒）
- 支持 `npm start`、`python app.py`、`docker-compose up` 等命令
- 纯HTML应用留空，走内置预览服务器

**全局蓝本优先级UI**
- 蓝本列表顶部固定 🌍 全局蓝本选项（默认勾选）
- 勾选全局 → 局部蓝本全部灰化禁用 + 悬浮提示
- 取消全局 → 局部蓝本恢复可选
- 全选按钮只影响局部蓝本

**编程AI智能审视蓝本**
- copilot-instructions新增规则：代码变化时自动审视并更新对应模块蓝本
- 6种触发条件（覆盖Web/小程序/手机/桌面）：
  1. 新增/删除UI元素
  2. 修改元素ID/class/选择器
  3. 修改文本内容
  4. 修改业务逻辑
  5. 修复Bug
  6. 修改应用配置（URL/端口/路由/权限/启动入口）
- 其他情况（CSS/注释/重构）自动跳过，节省token

**插件UI优化**
- 修复侧边栏窄宽度下蓝本列表溢出问题
- 文字单行省略 + overflow-x:hidden
- 蓝本生成提示词更新：要求按模块拆分 + 填写start_command

#### 测试数据
- 总测试数：940 -> 947（新增7个批量蓝本测试）
- 全部通过

#### Git提交
- c0c9d78: 蓝本统一放testpilot/文件夹+修复UI溢出
- 802b127: start_command自动启动+预览服务器适配
- 20ead48: 全局蓝本优先级UI+互斥逻辑
- fbd7f73: copilot-instructions更新+编程AI自动审视蓝本
- 4d823e0: 简化蓝本检查规则-只列出需要检查的6种情况
- 3c33dca: 蓝本检查规则扩展-全平台覆盖

---

### v10.2+ 微信小程序盲测实战验证（2026-03-07）

#### FreshMart小程序盲测项目

**项目概述**：创建了一个含6个隐藏Bug的微信小程序（FreshMart生鲜超市），用盲测脚本模拟LLM生成测试并执行，验证小程序自动化测试能力。

**小程序结构**：
- `miniprogram-demo/`：5个页面（首页/搜索/购物车/结算/订单），8种商品，6个预埋Bug
- `miniprogram-demo/testpilot/testpilot.json`：蓝本（只描述功能需求，不提Bug）
- `run_blind_test.js`：盲测脚本v5（模拟AI生成的12个测试场景）

**盲测v5最终结果（commit 0967f58）**：
- **12场景全部跑通，5真Bug / 7通过 / 0误报，耗时79.8秒，12张截图**
- 🐛 会员价格：vipPrice用了0.85折而非0.8折（差0.63元）
- 🐛 库存限制：addToCart只检查stock<=0，不检查购物车已有数量
- 🐛 搜索排序：String.localeCompare做数字排序（字符串排序）
- 🐛 优惠券满减：判断条件是 `>` 而非 `>=`（小计=门槛时不能用）
- 🐛 配送费：同城配送写了5元而非0元
- ✅ 售罄商品 / 购物车精度 / 浮点精度 / 列表完整性 / 空购物车 / 搜索 / 分类筛选

#### 关键技术发现（极其重要！）

**SDK导航方法全部超时，evaluate调用原生API又快又稳：**
| 方式 | 耗时 | 可用 |
|------|------|------|
| `mp.navigateTo()` SDK封装 | 10秒超时 | ❌ |
| `mp.navigateBack()` SDK封装 | 10秒超时 | ❌ |
| `mp.reLaunch()` SDK封装 | 10秒超时 | ❌ |
| `evaluate(()=>wx.navigateTo())` 原生 | 64ms | ✅ |
| `evaluate(()=>wx.navigateBack())` 原生 | 25ms | ✅ |
| `evaluate(()=>wx.reLaunch())` 原生 | 41ms | ✅ |

**重要规则（必须遵守）**：
1. **绝不使用SDK导航方法**：`mp.navigateTo()`等全部超时，必须用`evaluate(wx.xxx())`
2. **开头重启1次，中途不重启**：`cli close` + `cli open` + `cli auto --auto-port 9420`，之后靠`evaluate(wx.reLaunch)`回首页
3. **场景独立**：每个场景前用`evaluate`清空globalData + `reLaunch`回首页
4. **截图留证**：每个场景用`mp.screenshot()`截图保存
5. **重试机制**：元素找不到时等1.5秒重试最多3次，消除渲染延迟导致的误报
6. **自动化端口固定9420**：`--auto-port 9420`，不是HTTP服务端口

**导航研究独立项目**：`nav-research/`目录，可复制到其他电脑验证导航方案

**提示词已更新**：`src/prompts/miniprogram_test_guide.md` 写入了上述所有经验和最佳实践

#### 插件当前状态分析

**已确认完成**：
- ✅ "全选按钮"已移除，改为"🌍 全局蓝本"复选框（默认勾选，更直观）
- ✅ 蓝本模式+探索模式双Tab
- ✅ 一键启动/断开引擎
- ✅ 蓝本列表显示平台图标（🌐Web/💬小程序/📱手机/🖥️桌面）

**待改进（下一步）**：
1. 蓝本按项目分组显示（多项目workspace下蓝本混在一起）
2. 运行时根据蓝本`platform`字段走不同执行路径（Web→Playwright，小程序→automator桥接）
3. 多项目选择器（用户先选项目再选蓝本）

---

#### 下一步（v10.3）

**蓝本功能描述模式 — 已完成（v10.1就有了）**
- `BlueprintGenerator.from_url(url, description="这是电商后台，有登录/商品/订单")` 已实现
- `POST /api/v1/blueprint/generate` + MCP工具 `generate_blueprint` 均已上线
- 不是"下一步"，是已完成功能

**v10.3 插件改造：项目选择器+平台自动路由（真正的下一步）**

核心改动（详见项目规划v10.3）：
1. **项目下拉框**：引擎状态栏右侧新增，自动扫描workspace中含蓝本的项目
   - 无项目 → 显示"暂无测试项目"+引导文字
   - 单项目 → 自动锁定不可展开
   - 多项目 → 下拉选择，格式：`项目名 + 平台徽章`（如 `FreshMart 💬`）
2. **蓝本列表联动**：切换项目后自动过滤只显示该项目的蓝本
3. **平台自动路由**：用户点"开始测试"，插件根据蓝本`platform`字段自动走对应引擎
   - `web` → Playwright / `miniprogram` → automator桥接 / `android` → Appium / `desktop` → Win32
4. **扫描时机**：插件激活时 + 引擎连接时 + 用户点刷新🔄（不轮询）
5. **小程序集成**：自动启动`cli auto --auto-port 9420`+桥接服务器

实现6步：扫描重构→下拉框UI→列表联动→平台路由→小程序启动→空项目引导

**v10.3 实施记录（2026-03-07）**

**根因发现**：插件UI从v10.2起就有隐藏Bug——HTML中缺少`btnScanBp`和`btnBrowseBp`两个按钮元素，但JS代码引用了它们。`getElementById("btnScanBp").addEventListener(...)` 抛null异常后，**后续所有JS代码不执行**（包括引擎检查、蓝本扫描、按钮显示逻辑）。用户一直没重装vsix所以没发现。

**修复+改造（最小改动策略）**：
1. ✅ 修复根因：HTML补上`btnScanBp`+`btnBrowseBp`按钮
2. ✅ 后端扫描重构：`_handleScanBlueprints`按项目文件夹分组返回`{projectDir, projectName, platform, blueprints[]}`
3. ✅ 项目下拉框UI：引擎状态下方新增`<select>`+🔄刷新按钮
4. ✅ 蓝本列表联动：`onBlueprintList`处理分组数据，`renderBlueprintList`独立函数，切换项目自动过滤
5. ✅ 平台自动路由：蓝本测试按钮传递`platform`参数（web/miniprogram/android/desktop）
6. ✅ 引擎连接自动扫描：连接成功时自动`postMessage({ command: "scanBlueprints" })`
7. ✅ 空项目引导：无蓝本时显示引导文字

**经验教训**：
- 插件HTML里的DOM元素必须和JS的`getElementById`严格对应，缺一个就导致后续全崩
- 改造插件时要用最小改动策略，不要大规模重写
- 回滚只影响`sidebarProvider.ts`一个文件，核心引擎和测试成果完全不受影响

**v10.3 Phase1-3 完成（2026-03-08）**

基于用户产品规范，完成插件UI专业化改造：

**Phase 1: 项目隔离优先**
- ✅ 删除"全部项目"选项，永不跨项目批跑
- ✅ 项目下拉框只显示单个项目列表，用户一次只选一个项目
- ✅ 项目显示格式：`项目名（平台全称）`，如 `FreshMart 生鲜超市（微信小程序）`
- ✅ 平台全称映射：web→Web、android→Android、ios→iOS、miniprogram→微信小程序、desktop→Windows桌面

**Phase 2: Tooltip分区优先（操作提示与内容提示分离）**
- ✅ 局部蓝本拆分为两个区域：复选框区 + 内容区，各自独立title
- ✅ 悬停复选框区域：显示操作提示（"已启用全局蓝本，如需局部选择请先取消全局蓝本"）
- ✅ 悬停内容区域：始终显示蓝本详情（描述、场景数、步骤数、路径）
- ✅ 全局勾选时只灰化opacity，不再一刀切覆盖内容区tooltip

**Phase 3: 平台前置检查优先（运行前置检查+阻断逻辑）**
- ✅ 新增`_handlePlatformPrecheck`后端方法，按平台执行前置检查
- ✅ Android/iOS：检测设备连接 → 未连接时阻断并提示 → 已连接时自动创建会话
- ✅ 微信小程序：检测开发者工具状态 → 未就绪时阻断并提示固定端口9420
- ✅ Web/Windows桌面：直接通过检查
- ✅ 前端流程：点击"运行选中蓝本" → 先执行`platformPrecheck` → 检查通过后才发送测试请求
- ✅ `engineClient.ts`新增多平台API：
  - `startMobileBlueprintTest()` - 手机蓝本测试
  - `startMiniprogramBlueprintTest()` - 小程序蓝本测试
  - `startDesktopBlueprintTest()` - 桌面蓝本测试
  - `listMobileDevices()` - 列出已连接移动设备
  - `listMobileSessions()` - 列出活动移动会话
  - `createMobileSession()` - 创建移动会话
  - `getMiniprogramDevtoolsStatus()` - 检查小程序开发者工具状态

**改动文件**：
- `extension/src/sidebarProvider.ts`：项目隔离+Tooltip分区+平台前置检查
- `extension/src/engineClient.ts`：多平台API方法

**Git提交**：
- commit e04862e: v10.3 Phase1-3完成

**v10.3 补充修复（2026-03-08 上午）**

1. ✅ 紧急修复：恢复被误删的`btnCheckEngine`按钮（删HTML元素导致JS全崩，同一Bug模式第2次）
2. ✅ 隐藏"检查连接"按钮：引擎状态已实时显示连接/未连接，该按钮多余。用CSS `class="hidden"`隐藏
3. ✅ 新增`android-demo/`和`ios-demo/`蓝本项目，验证设备检测功能
4. ✅ iOS项目在Windows下灰色不可选，附温馨提示"— 需macOS环境"
5. ✅ Android项目显示设备状态行：✅已连接 / ❌未检测到设备 + "检测设备"按钮
6. ✅ iOS在Windows下显示🍎"iOS项目需要macOS环境才能运行测试"
7. ✅ 修复设备状态行布局：改为上下两行（状态文字+按钮），解决侧边栏窄时竖排挤压
8. ✅ 新建`.windsurf/rules.md`铁律文件（7条铁律，每次会话自动全量加载）
9. ✅ 复选框tooltip初始状态修复

**Git提交**：
- bdd0c3d: 紧急修复恢复btnCheckEngine
- 4795b54: 隐藏检查连接+demo项目+iOS灰色+设备状态行
- 7e5fd64: rules.md铁律文件+项目规划更新
- 5c4788a: 设备状态行布局修复

**产品设计讨论结论（已写入项目规划）**：
- 记忆存储三层策略：rules.md铁律(保底) → 备忘录(详情) → AI记忆(辅助)
- 跨项目经验：标注来源+仅供参考，不需AI自动分层
- 蓝本介入：关键节点触发，Token消耗接近零
- iOS策略：Windows下灰色不可选+友好提示

---

**v10.3 小程序自动化连接修复（2026-03-08 下午）**

**问题**：小程序蓝本测试报"桥接服务器启动超时"，连接automator失败。

**根因分析**：
1. 旧版`launch()`在启动时做`cli close`+`cli open`重启，导致模拟器重新初始化（截图显示"模拟器启动失败: TypeError: Failed to fetch"）
2. 桥接服务器启动时在`server.listen`回调里做6次重试连接（阻塞18秒），Python端轮询HTTP时`connected`一直是false
3. Python端只检查`connected=true`，但HTTP服务器虽然在监听却还没连上automator

**关键端口发现（极其重要！）**：
| 端口 | 用途 | 来源 |
|------|------|------|
| **49492** | 开发者工具HTTP服务端口（CLI命令用） | 设置→安全设置→服务端口 |
| **9420** | automator WebSocket端口（自动化用） | `cli auto --auto-port 9420`指定 |
| **9421** | 桥接服务器HTTP端口（Python↔Node通信） | 代码固定 |

**这三个端口是不同的！** `--auto-port 9420`确实有效，不要和服务端口49492混淆。

**修复方案（launch() v8.3 五阶段流程）**：
```
阶段1: cli auto（不重启！避免模拟器崩溃。项目未打开才open+auto）
阶段2: TCP探测WS端口9420就绪（最多20秒）
阶段3: 启动桥接HTTP服务器（不自动连接automator）
阶段4: 等HTTP服务器响应（最多5秒，不管connected状态）
阶段5: POST connect命令主动触发连接（重试8次×5秒=40秒）
```

**修复结果**：
```
cli auto 完成 (rc=0)
WS端口 9420 已就绪（第1秒）
HTTP服务器已就绪（0.5秒）
小程序自动化已连接 | 端口:9420 | 第1次  ← 一次就连上！
```

**修改文件**：
- `src/controller/miniprogram.py`：重写launch()五阶段+_run_cli()+_check_tcp_port()+_kill_bridge()
- `src/controller/miniprogram_bridge_server.js`：v8.3启动时不自动连接，等POST connect命令

**Git提交**：e6aca51

**铁律补充**：
- **不要在launch()里做cli close+cli open重启**：会导致模拟器重新初始化失败
- **桥接服务器启动时不要自动连接automator**：让Python端通过POST主动触发，这样HTTP能立即响应健康检查
- **服务端口≠自动化端口**：设置里的49492是HTTP服务端口，9420是automator端口，别混淆

---

### v10.4 砍掉桥接服务器+执行器模式（2026-03-08 下午）

**架构变更**：砍掉桥接服务器（`miniprogram_bridge_server.js`），改用一次性Node.js执行器（`miniprogram_runner.js`）。

**新架构**：
```
插件 → Python API → 写JSON步骤文件 → node miniprogram_runner.js → 执行步骤 → 输出JSON → Python解析返回
```
跟`run_blind_test.js`一模一样的模式：Node.js脚本自己做所有事情，跑完就退出。没有长连接、没有桥接服务器、没有端口占用问题。

**三级连接策略**（`miniprogram_runner.js`）：
```
策略1: 直接connect（模拟器已正常+auto已启动时，0.1秒秒连）
策略2: 只执行cli auto → connect（不close不open）
策略3: cli open + cli auto → connect（不做close！）
```

**关键发现和铁律**：

1. **绝对不能cli close**：`cli close`会破坏模拟器状态，之后open回来的模拟器永远是"启动失败: TypeError: Failed to fetch"，连`cli cache --clean compile`都救不回来
2. **开发者工具进程残留问题**：`cli quit`不能杀干净所有进程，必须用`Get-Process wechatdevtools | Stop-Process -Force`彻底杀掉后才能干净重启
3. **模拟器"启动失败"的修复流程**：
   ```
   ① Get-Process wechatdevtools, "微信开发者工具" | Stop-Process -Force
   ② 等5秒
   ③ cli open --project "项目路径"
   ④ 等8秒
   ⑤ cli auto --project "项目路径" --auto-port 9420
   ⑥ 确认输出 √ auto
   ```
4. **run_blind_test.js会断开auto**：该脚本做了`close→open→auto`重连，跑完后`mp.disconnect()`会断开automator连接，后续其他脚本策略1直连会失败
5. **SDK方法全部超时**：`mp.navigateTo()`, `mp.reLaunch()`等SDK封装方法会10秒超时，必须用`evaluate(() => wx.navigateTo({url:...}))`原生API调用
6. **PowerShell Out-File会加BOM头**：导致Node.js的`JSON.parse`失败，需要`replace(/^\uFEFF/, '')`去BOM
7. **项目列表重复**：扫描蓝本时可能存在重复加载，需要去重

**修改文件**：
- `src/controller/miniprogram_runner.js`（新增）：一次性Node.js执行器，三级连接策略
- `src/api/routes.py`：小程序蓝本测试改用执行器模式，不再依赖MiniProgramController
- `src/controller/miniprogram_bridge_server.js`（保留但不再使用）

**Git提交**：e857d4f

---

## v10.5 — 小程序蓝本测试完整闭环（2026-03-08）

### 成果
- **100%通过率**：70/70步骤，0 Bug，44.4秒完成
- **通用执行器**：13种步骤类型，不写死任何业务逻辑
- **完整12场景蓝本**：全部用通用步骤表达，编程AI可按此格式生成任意蓝本
- **Bug发现→修复→验证闭环**：发现4Bug→修复2个小程序真实Bug+2个蓝本问题→重测100%

### 关键技术发现和铁律

1. **小程序evaluate必须用`new Function`传函数**：
   - `mp.evaluate("字符串")` → 报`Arg string terminates parameters early`或`Unexpected token`
   - `mp.evaluate(new Function("代码"))` → 正确执行，等同于`mp.evaluate(() => { 代码 })`
   - 这是因为automator对字符串参数有严格的解析限制

2. **小程序没有`document`对象**：
   - evaluate里不能用`document.querySelector`，只能用`getApp()`、`getCurrentPages()`、`wx.xxx`
   - 查询DOM元素用automator的`page.$`和`page.$$`（在Node端执行，不在小程序端）
   - 新增`page_query`步骤类型解决此问题

3. **`callMethod('onShow')`会导致100秒超时**：
   - reset_state里调callMethod会让后续截图卡死
   - 解决：去掉callMethod，`reLaunch + sleep(2000)`已足够

4. **策略3必须强杀进程，不能依赖`cli quit`**：
   - 模拟器状态烂了时，`cli quit`也会失败
   - 改为`taskkill /F /IM wechatdevtools.exe /T`强杀所有进程
   - 然后`cli open` → `cli auto`重建干净环境

5. **小程序代码修改后必须重新编译**：
   - 外部编辑器改了js文件，开发者工具不会自动检测
   - 需要在开发者工具里点"编译"，或者执行器自动kill重启

### 通用执行器步骤类型一览

| 步骤类型 | 用途 | 执行环境 |
|---------|------|---------|
| `navigate` | reLaunch跳页面（清空栈） | 小程序端 |
| `navigate_to` | navigateTo跳页面（保留栈） | 小程序端 |
| `click` | 点击元素 | automator |
| `tap_multiple` | 连续点击N次 | automator |
| `fill` | 输入文本 | automator |
| `screenshot` | 截图 | automator |
| `evaluate` | 执行JS（new Function） | 小程序端 |
| `call_method` | 调用页面方法 | automator |
| `page_query` | 查元素(text/count/texts) | Node端 |
| `read_text` | 读取元素文本（带重试） | automator |
| `assert_text` | 文本包含断言 | automator |
| `assert_compare` | 数值比较断言 | automator |
| `reset_state` | 重置全局状态+回首页 | 小程序端 |
| `wait` | 等待 | Node端 |
| `scroll` | 滚动页面 | 小程序端 |

### 修改文件
- `src/controller/miniprogram_runner.js`：13种步骤类型、new Function方案、策略3强杀进程
- `src/api/routes.py`：返回bugs和steps详情、crash保护友好提示
- `src/testing/models.py`：ActionType枚举新增7种类型
- `extension/src/sidebarProvider.ts`：步骤详情自动展开
- `miniprogram-demo/testpilot/testpilot.json`：v3.1完整12场景蓝本
- `miniprogram-demo/pages/index/index.js`：修复库存上限检查Bug
- `miniprogram-demo/pages/checkout/checkout.js`：修复同城配送费Bug

### Git提交链
- 52c20cf：通用执行器+完整12场景蓝本
- 62d8289：修复evaluate用document问题+page_query+read_text重试
- 6b4651b：关键修复evaluate/navigate/reset_state全部改用new Function
- 3970581：修复小程序蓝本测试返回bugs和steps详情
- c64f8ed：修复2个小程序Bug（库存+配送费）+空购物车蓝本
- 92ecee3：执行器crash保护
- 444c59e：策略3改为强杀进程taskkill
- 3cc6952：步骤详情自动展开+逐步进度输出

---

### 下一步目标（v10.6）

#### 优先级1：测试体验
1. **逐步进度日志**：routes.py改Popen流式读stderr，实时推送每步进度
2. **测试中状态提示**：前端显示"测试中..."动画，让用户知道系统在运行

#### 优先级2：端到端验证
3. **Windows桌面蓝本实测**（desktop-demo）
4. **Android真机蓝本实测**（android-demo）

#### 优先级3：蓝本生成提示词 ✅ 已完成
5. **提示词文档**：`docs/miniprogram-blueprint-prompt.md`
   - 15种步骤类型完整说明
   - 7条铁律（evaluate格式、无document、reset_state必须首步等）
   - 6个踩坑记录（附错误信息和修复方案）
   - 执行环境对照表
   - 蓝本生成任务模板

#### v10.5 最终成果（2026-03-08）
- **地雷挖掘验证**：带Bug代码跑蓝本 → 97%通过率，精准挖出2个地雷（库存Bug+配送费Bug）
- **AI生成蓝本验证**：提示词+wxml/js文件 → AI生成11场景74步骤 → 100%通过
- **evaluate自动return**：`new Function`包裹的IIFE/表达式自动加`return`前缀
- **前端进度提示**：测试中每5秒跳动"正在连接模拟器→正在执行步骤→仍在测试中"
- **蓝本清理**：每平台保留最复杂一本（Web/小程序/桌面/Android/iOS各1本）
- **项目规划更新**：v11.0规划（桌面实测→Android实测→商业化→API开放→发布）

#### 插件增强（2026-03-08 下午）
- **扫描排除test_fixtures**：`findFiles`排除模式加入`test_fixtures/`和`.venv/`
- **提示词按平台区分**：option加`data-platform`属性，按钮读取platform后生成对应提示词
  - Web：模块拆分+CSS选择器+start_command
  - 小程序：7条铁律+15种action+evaluate用IIFE+无document+reset_state
  - Android：手机浏览器CSS / 原生App用resource-id
  - 桌面：4种选择器（name/automationid/class/point）
- **提示词补强"测试设计黄金8规则"**：
  1. 功能全覆盖（数按钮数对蓝本数）
  2. 操作→断言配对（没有断言的操作等于没测）
  3. 业务流程端到端串联（≥3个页面/功能）
  4. 状态变化验证（操作前后对比）
  5. 异常和边界测试（空提交/超长/特殊字符）
  6. 弹窗和提示验证（成功/错误/确认消息）
  7. 选择器规范（真实id/class，禁脆弱选择器）
  8. 启动命令（start_command字段）
- **Web测试失败根因**：test_fixtures/buggy_shop的base_url=localhost:8901没启动 → Connection closed。排除后不再出现

#### Android移动商城demo + 蓝本验证（2026-03-08 下午）
- **新建mobile-demo**：鲜果坊移动H5商城（登录/注册/商品分类搜索/购物车/结算/订单/个人中心）
- **预埋5个Bug**：密码任意登录/库存无限加入/浮点精度/自提收费/优惠未生效
- **蓝本28场景208步骤**：严格按黄金8规则编写，覆盖所有功能+异常+端到端
- **PC端测试结果**：99%通过率（206/208），精准挖出2个Bug（自提配送费+首单优惠未生效）
- **提示词统一**：BlueprintGenerator/API/复制按钮共用 `get_blueprint_prompt_for_platform(platform)`
  - `prompts.py` 新增 `BLUEPRINT_GOLDEN_RULES` + 4个平台常量
  - `GenerateBlueprintRequest` 增加 `platform` 字段
  - 复制按钮读取 `projectSelect.selectedOptions[0].dataset.platform`

#### v10.3 截图策略修复+蓝本模块拆分验证（2026-03-08 下午）

**黄金规则扩展至13条**
- 规则9：深度Bug挖掘（AI充当安全审计师/架构师角度审视代码）
- 规则10：参考功能文档（如项目有功能说明文档，AI需参考）
- 规则11：截图策略（每个蓝本模块的第1个场景末尾加1张screenshot，其余不加）
- 规则12：蓝本按功能模块拆分（按业务功能而非页面数量拆分，如auth/home/cart/checkout/me）
- 规则13：蓝本增量修改（在原蓝本基础上修改，不重新生成覆盖）
- 三处同步更新：`prompts.py`、`sidebarProvider.ts`、`docs/blueprint-prompt-golden-rules.md`

**引擎Bug修复（commit 2115af2）**
- **截图过多**：`blueprint_runner.py`和`mobile_blueprint_runner.py`每步都截图 → 改为按需截图（仅蓝本写了`screenshot`或有`expected`时）
  - 修复前：251步=251张截图+大量无用AI分析请求
  - 修复后：仅5张截图（每个模块第1场景1张）+ 断言失败自动截图
- **Session残留**：安卓项目切换后旧session在内存中残留但已失效 → `AndroidController`新增`is_session_alive()`方法，`list_mobile_sessions`自动检测并清理失效session

**蓝本功能模块拆分验证**
- mobile-demo原始蓝本拆为5个独立模块：
  - `auth.testpilot.json`（登录注册，61步）
  - `home.testpilot.json`（首页商品，36步）
  - `cart.testpilot.json`（购物车，55步）
  - `checkout.testpilot.json`（结算，61步）
  - `me.testpilot.json`（个人中心，38步）
- PC测试结果：**249/251通过（99.2%）**，2个"失败"是成功发现的隐蔽Bug（BUG4自提隐藏收费）

**Android真机测试结论**
- `mobile-demo`是网页项目，PC端Playwright已100%覆盖，手机浏览器跑网页无增量价值
- 真机测试的意义场景：原生APK（Java/Kotlin）、Flutter、React Native
- 决策：跳过，等有原生Android项目再做真机测试

**铁律**
- ❌ 引擎不得在每步执行后无条件截图，只在蓝本写了`screenshot`或有`expected`时才截
- ❌ 不得在手机浏览器上跑纯网页项目测试（PC端已覆盖，无增量价值）
- ✅ 蓝本按业务功能模块拆分，每个模块可独立测试也可批量测试

---

## 十、Android 原生应用测试（Flutter）调试记录（2026-03-09）

### 10.1 测试环境

| 项目 | 值 |
|------|-----|
| 被测应用 | Flutter Demo (`com.testpilot.flutter_demo/.MainActivity`) |
| 路由 | `/`(LoginScreen) → `/tasks`(TasksScreen)，`pushReplacementNamed`无回退栈 |
| 设备 | vivo V2243A, Android 15, Serial `10AD6Y2XL90030M` |
| Appium | v3.2.0, 端口 4723, uiautomator2@7.0.0 |
| TestPilot | Python 3.10 venv, 端口 8900, model=doubao-seed-1-8-251228 |
| 蓝本 | `flutter-demo/testpilot/auth.testpilot.json` v1.3, 3场景21步 |

### 10.2 Flutter UI特性（极其重要）

**Flutter渲染出来的Android无障碍树有独特结构：**

1. **所有元素的`text`属性都为空（`text=""`）**，文本内容全部在`content-desc`属性中
2. **按钮是双层结构**：外层`content-desc="btn_login"`(clickable=false) 包裹内层`content-desc="登录"`(clickable=true)
3. **`accessibility_id`策略找到元素后，UiAutomator2会调`waitForIdle()`**，Flutter动画循环会导致该调用永远阻塞

### 10.3 已解决问题（Scene 1 全部通过：7/7步，<8秒）

| 问题 | 解决方案 |
|------|---------|
| `accessibility_id`策略hang | 转换为`-android uiautomator`策略：`new UiSelector().description("xxx")`绕过`waitForIdle()` |
| Flutter动画阻塞UiAutomator2 | Session创建后设`waitForIdleTimeout=0, waitForSelectorTimeout=0` |
| DialogDismisser与测试竞争 | 测试开始前`self._ctrl._dialog_dismisser.stop()`停止轮询 |
| Flutter键盘遮挡 | `adb shell input keyevent 111`(ESCAPE)收起键盘 |
| `_request()`无超时捕获 | 添加`(TimeoutError, OSError)`异常捕获 |

### 10.4 核心未解决问题

**现象：Scene 2/3 的 `navigate` 后，`findElement` 永远超时（每次请求恰好5秒）**

- Scene 1 使用初始Session的`launch()` → findElement瞬间返回（0.1秒）
- Scene 2/3 的`navigate`需要重启应用回到登录页 → 重建Session后findElement永远hang 5秒
- 最终结果：**71% (15/21)**，所有元素交互步骤在navigate之后全部失败

**5秒超时来源**：`_find_element()` 的HTTP timeout=5 → `_session_request("POST", "/element", ..., timeout=5)`

**16.6秒总超时算术**：`wait_for_element(timeout_ms=15000)` → 3次重试 × (5s HTTP + 0.5s sleep) = 16.5s ≈ 16.6s

### 10.5 Session重建机制（`_rebuild_session_and_launch()`）

当前`navigate()`中的7步重建流程：
```
1. adb force-stop com.testpilot.flutter_demo（杀应用）
2. DELETE /session/{sid}（3秒超时，失败也继续）
3. am force-stop io.appium.uiautomator2.server + .test（杀UiAutomator2进程）
4. asyncio.sleep(1)（等进程退出）
5. POST /session 创建新Session（与launch()相同capabilities）
6. POST /appium/settings 设 waitForIdleTimeout=0, waitForSelectorTimeout=0
7. adb am start -n com.testpilot.flutter_demo/.MainActivity + asyncio.sleep(3)
```

**Session重建本身是成功的**（可以看到新的Session ID），但新Session创建后的findElement永远hang。

### 10.6 已排除的方案（Dead Ends，全部实验验证过）

| # | 方案 | 实验结果 | 排除原因 |
|---|------|---------|---------|
| 1 | 旧Session残留导致冲突 | 完全清理所有adb forward + 杀全部进程 + 从零创建Session → 仍然hang | 即使干净环境也复现 |
| 2 | adb forward端口冲突 | 发现3个forward都映射tcp:6790（8200/8201/8203），`adb forward --remove-all`全清 → 仍然hang | 端口冲突不是根因 |
| 3 | UiAutomator2 Server进程残留 | `am force-stop`后`ps -A \| grep uiautomator`确认进程已消失 → 新Session仍然hang | 进程确实被杀了 |
| 4 | terminateApp/activateApp API | 通过率降至67%（更差），terminateApp自身hang 22秒 | Appium内置API更不稳定 |
| 5 | `--activity-clear-task`启动参数 | 结果与不加参数一样71% | Flutter单Activity不受影响 |
| 6 | `--activity-clear-top`启动参数 | Flutter不重置路由，停留在/tasks页 | Flutter的pushReplacementNamed不走Android标准栈 |
| 7 | POST /appium/settings刷新 | Settings请求本身hang 5秒 | 不是findElement的问题，是UiAutomator2 Server整体不响应 |
| 8 | POST /source获取页面源码 | Source请求本身也hang 5秒 | 同上，Server整体不可用 |
| 9 | 不带appPackage创建Session + 手动adb start | Session创建成功、Settings成功 → 手动启动app后findElement仍然hang | app启动才导致Server hang |

### 10.7 关键矛盾（最重要的线索）

**第一次Session（launch()）→ 一切正常，findElement瞬间返回**
**任何后续Session（即使从完全干净状态创建）→ findElement永远hang**

而`adb uiautomator dump`从ADB命令行仍能正常获取所有元素 → Android无障碍树本身是好的，问题在UiAutomator2 Server的HTTP处理线程。

**核心疑问**：第一次Session和后续Session之间，设备上到底有什么不可逆的状态变化？

可能方向：
- Appium的UiAutomator2驱动在首次Session时的instrument命令参数可能不同
- UiAutomator2 APK安装行为（首次安装 vs 已存在时的行为差异）
- vivo设备级安全机制（限制后续instrumentation？）
- Flutter引擎在第二次被force-stop后重新启动时的无障碍服务注册时机不同

### 10.8 诊断实验时间线（2026-03-09 下午）

```
16:33:29  实验A：带appPackage创Session 286e39bd → Settings超时5s → findElement超时10s
16:34:30  实验A2：带appPackage从force-stop → Session cd2489be → Settings成功1s → 被中断
16:35:49  实验A3：Session ae3c7ef6 → Settings超时5s（旧Session未删导致冲突）
16:36:00  发现：adb forward --list 有3个残留forward → tcp:8200/8201/8203全映射tcp:6790
16:36:48  实验B：不带appPackage → Session 8370de4e → Settings成功 → 手动adb start → findElement仍超时
16:41:02  干净实验：remove-all + 杀全部进程 → Session a2879b34(9s) → findElement btn_login → 仍超时！
```

**结论**：所有外部因素（端口/进程/旧Session）都已排除，问题是UiAutomator2 Server与Flutter运行时之间的内部交互。

### 10.9 下一步调试方向

1. **对比首次/后续Session的instrument命令**：用`adb logcat -s Appium`观察Appium在设备端执行的具体指令差异
2. **尝试不杀app只重建Session**：观察在app保持运行时创建新Session的行为
3. **尝试Appium的`noReset: true`/`fullReset: true`**：控制UiAutomator2 APK的安装行为
4. **Python诊断脚本**（替换PowerShell避免输出截断）：精确计时每一步操作

### 10.10 3月10日诊断（v1-v9实验，根因定位+修复）

#### 实验v1: 不杀app只重建Session（方向A）
- **方法**：保持app运行，只 DELETE 旧Session + POST 新Session
- **结果**：❌ findElement 仍然 hang 10s
- **结论**：问题不在Session重建本身

#### 实验v2: 不force-stop只发am start（方向A2）
- **方法**：不 force-stop，直接 `am start -n component` 发 ACTION_MAIN
- **结果**：❌ findElement hang，且 U2 直连端口(8200)变为**完全不可达**
- **发现**：`adb uiautomator dump` 返回 XML 长度=0 — 无障碍树完全空
- **结论**：am start 会破坏 U2 Server 与app的连接

#### 实验v5: Flutter UI树结构分析
- **重大发现 — Flutter Semantics → Android 无障碍属性映射规则**：

| Flutter Semantics 属性 | Android 无障碍属性 | 示例 |
|---|---|---|
| `Semantics(label:"xxx", button:true)` | `content-desc="xxx"` | btn_login → `content-desc="btn_login"` |
| `Semantics(label:"xxx", textField:true)` | `hint="xxx"` | tf_username → `hint="tf_username"` |
| `IconButton(tooltip:"xxx")` | `tooltip-text="xxx"` | logout → `tooltip-text="logout"` |
| `Semantics(label) + child有值` | `content-desc="label\n值"` | txt_pending → `content-desc="txt_pending\n待办任务：3项"` |

- **正确选择器**：
  - content-desc → `UiSelector().description("btn_login")`
  - hint → XPath `//android.widget.EditText[@hint="tf_username"]`
  - tooltip-text → XPath `//*[@tooltip-text="logout"]`
  - 合并内容 → `UiSelector().descriptionContains("txt_pending")`

#### 实验v7: 精确定位hang触发条件（关键突破）

**D1实验（不填表单，直接空登录）**：
```
btn_login     → ✅ 0.1s
click登录      → ✅ 0.1s (空密码→跳tasks)
getPageSource → ✅ 0.1s (tasks页)
txt_pending   → ✅ 0.0s
logout        → ✅ 0.3s (XPath @tooltip-text)
click logout  → ✅ 0.1s
btn_login     → ❌ HANG 5.0s (回到login页后)
```

**D2实验（填表单，密码错误不跳转）**：
```
btn_login     → ✅ 0.1s
tf_username   → ✅ 0.2s (XPath @hint)
fill admin    → ✅
getPageSource → ✅ 0.1s (fill后U2正常!)
tf_password   → ✅ 0.1s
fill 123456   → ✅
getPageSource → ✅ 0.1s (fill密码后U2正常!)
click登录      → ✅ 0.2s (密码错→setState显示错误文本)
getPageSource → ❌ HANG 8.0s (setState后U2无响应!)
```

**关键结论**：
1. **fill操作不会导致U2 hang**（fill后getPageSource正常）
2. **Flutter setState() 导致U2无响应**（D2: 密码错误→setState→hang）
3. **Flutter pushReplacementNamed() 导致U2无响应**（D1: logout→hang）
4. 第一次pushReplacementNamed(login→tasks)正常，后续操作触发U2无响应

#### 实验v8: U2 hang后的恢复手段验证

**D1路径复现hang后，逐项检查**：

| 检查项 | 结果 | 含义 |
|---|---|---|
| U2进程(`ps -A \| grep uiautomator`) | **(无进程)** | U2被**SIGKILL杀死** |
| `adb uiautomator dump` | rc=137 | 确认被信号9强杀 |
| U2直连端口(8200) | timeout | 进程没了 |
| 重设 waitForIdleTimeout=0 | timeout 3s | 对死进程无法操作 |
| `adb input tap` | ✅ 能用 | 设备本身正常 |
| DELETE 旧Session | timeout 10s | Appium在等死掉的U2 |
| **⭐ 重建Session（不杀app）** | **✅ 7.9s** | 新U2实例完全正常 |
| 新Session找btn_login | **✅ 0.1s** | 完全恢复！ |

**根因确认**：Flutter的setState/pushReplacementNamed触发无障碍树变化，导致UiAutomator2 Server进程被系统杀死或进入hang状态。**重建Session即可恢复。**

#### 实验v9: 端到端恢复流程验证

**完整流程**：空登录→tasks→logout→U2无响应→杀U2+新Session→继续操作

```
1. btn_login      → ✅ 0.1s
2. click(空登录)   → ✅ 0.1s → tasks页
3. txt_pending    → ✅ 0.1s
4. logout         → ✅ 0.3s
5. click logout   → ✅ 0.1s → U2无响应
6. btn_login      → ❌ 3.0s (超时,U2无响应)
7. [RECOVER] 杀U2+新Session → ✅
8. btn_login      → ✅ 0.1s (恢复成功!)
9-12. fill admin/admin123 → ✅ 全部正常
13. click登录(正确密码) → ✅ 0.1s → tasks页
14. U2存活         → ✅
15. txt_pending   → ✅ 0.1s (二次登录后正常!)
```

**注意**：v9步骤6中U2进程**存活但不响应**（vs v8中进程被杀）。
U2无响应有两种模式：进程死亡(SIGKILL) 或 进程hang(deadlock)。两种情况下杀U2+重建Session都有效。

### 10.11 修复方案（已实现）

**核心思路**：超时即恢复 — 任何Appium请求超时时，不检查U2进程状态，直接杀U2+重建Session+重试。

**修改文件**：`src/controller/android.py`

**新增方法**：
1. `_is_u2_process_alive()` — 检测U2进程是否存活（辅助诊断用）
2. `_recover_u2_session()` — 不杀app重建Session（杀U2残留→创建新Session→设waitForIdleTimeout=0）
3. `_safe_session_call()` — async版_session_request，超时自动恢复+重试

**修改方法**：
1. `_find_element_with_wait()` — 超时时触发恢复（每次调用最多恢复1次），恢复后重置计时
2. `tap()` — click改用 `_safe_session_call`
3. `input_text()` — clear/value改用 `_safe_session_call`
4. `get_page_source()` — 改用 `_safe_session_call`
5. `get_text()` — 改用 `_safe_session_call`

### 10.12 死胡同汇总（3月10日新增 #10-#12）

| # | 方向 | 实验 | 结果 | 排除原因 |
|---|---|---|---|---|
| 10 | 保持Session只重启app(force-stop) | v1 | ❌ | 不是Session问题 |
| 11 | 不force-stop只发am start | v2 | ❌ | am start本身也破坏U2 |
| 12 | fill操作是否破坏U2 | v7-D2 | fill正常 | fill不是原因，setState才是 |

### 10.13 代码变更清单（2026-03-10）

**`src/controller/android.py`（核心修改）**：
1. `launch()` — 新增 force-stop APP + U2 再创建 Session（解决 Flutter 动画导致首次 XPath hang）
2. `_find_element()` — `accessibility_id:` 策略从 `description()` 改为 `descriptionStartsWith()`
   - 原因：Flutter `Semantics(label) + child Text` 合并为 `"label\n子文本"`，精确匹配失败
3. `get_text()` — 从 `_find_element` 改为 `_find_element_with_wait(timeout_s=5)`
4. `_recover_u2_session()` — 新增，不杀 app 只重建 U2 Session
5. `_safe_session_call()` — 新增，任何请求超时自动恢复+重试
6. `_find_element_with_wait()` — 超时触发恢复（每次调用最多1次），恢复后重置计时

**其他文件修改**：
- `src/testing/blueprint.py` — validate() 原生 app 页面不要求 url；wait 动作校验放宽
- `src/api/routes.py` — Android 平台自动路由（AndroidController + MobileBlueprintRunner）
- `flutter-demo/testpilot/auth.testpilot.json` → v2.0 全面重写选择器
- `flutter-demo/testpilot/tasks.testpilot.json` → `@index` 改为 `@hint` XPath
- `.github/copilot-instructions.md` — 新增 Flutter Semantics→Android 映射表

### 10.14 测试结果（2026-03-10）

**首次测试**：75%通过率（15/21步），Scene 2/3 的 navigate 后全部超时
**最终测试**：**92%通过率（22/24步）**，103秒总耗时

| 步骤 | 场景 | 结果 | 分析 |
|------|------|------|------|
| Step 14 | 空登录→assert txt_error | ❌ | **正确检出 Bug-1**：空账号跳 tasks 页，txt_error 不存在 |
| Step 23 | 错密码→assert txt_error | ❌ | **引擎Bug**：恢复时 app 被重启，setState 状态丢失 |

### 10.15 引擎Bug修复（2026-03-11）

**问题**：`_recover_u2_session()` 创建新 Session 时传了 `appPackage`+`appActivity` → Appium 执行 `am start` 重启 Activity → Flutter 状态丢失。

**修复**：恢复 Session 时不传 `appPackage`/`appActivity`，Appium 只重启 U2 Server，不触碰 app。

**同步修复蓝本提示词三处不一致**：
1. `prompts.py` BLUEPRINT_PLATFORM_ANDROID — 从零补全 Flutter 规则
2. `docs/blueprint-prompt-android.md` — 删除错误的 `instance(N)`，改为 `@hint` XPath
3. `copilot-instructions.md` — 补充 wait 变体、Semantics label 覆盖 hintText 规则

**铁律**：
- ❌ `_recover_u2_session` 绝不传 appPackage/appActivity（防止状态丢失）
- ❌ Flutter TextField 绝不用 `instance(N)` — 必须用 `@hint` XPath
- ✅ 三处提示词（copilot-instructions / docs / prompts.py）必须保持同步

### 10.16 插件→引擎联调修复：APP不启动 + 停止无效（2026-03-10）

**现象**：
- 插件点击「运行蓝本测试」后，手机上 APP 根本没有打开，引擎疯狂重试 `//android.widget.EditText[@hint='tf_username']` 返回 404
- 点击「停止」按钮，HTTP 返回 200 OK，但测试继续运行不停止
- 用户只能通过停止引擎进程来终止测试

**根因分析（共3个Bug）**：

#### Bug 1：APP 从未启动

**链路追踪**：
1. 插件握手调用 `createMobileSession({ device_name: "V2243A" })` — **只传了 device_name**，没有传 `app_package` / `app_activity`
2. `routes.py create_mobile_session`：`MobileConfig(app_package=req.get("app_package", ""))` → 空字符串
3. `android.py launch()`：`if self._config.app_package and self._config.app_activity:` → **False** → `adb am start` 被跳过
4. APP 从未启动，手机停留在桌面/拨号器，所有元素查找失败

**修复方案**：在 `MobileBlueprintRunner.run()` 执行步骤前，从蓝本读取 `app_package` / `app_activity`，用 `adb shell am start -n {package}/{activity}` 强制启动 APP，等待 3 秒让 Flutter 渲染完成。

#### Bug 2：停止按钮无效

**链路追踪**：
1. 插件发送 `POST /api/v1/test/control?action=stop` → 调用 `test_controller.stop()`
2. `TestController.stop()` 内部检查：`if self._state == TestState.IDLE: return False`
3. `MobileBlueprintRunner` 从未调用 `test_controller.start()` → 状态永远是 IDLE
4. `stop()` 返回 False，`_cancelled` 永远不变，测试无法中断

**修复方案**：`MobileBlueprintRunner.run()` 开始时调用 `test_controller.start(total_steps=...)`，每步调用 `update_progress()` + `wait_if_paused()`，结束时调用 `on_test_complete()`。

#### Bug 3：步骤内重试循环无法中断

**问题**：即使 `_cancelled=True`，`_find_element_with_wait()` 内部有 15 秒重试循环，期间不检查取消标志，必须等循环结束才能响应停止。

**修复方案**：
1. `android.py.__init__` 新增 `self._is_cancelled_fn: object = None`
2. `_find_element_with_wait()` 循环顶部增加检查：`if self._is_cancelled_fn and callable(self._is_cancelled_fn) and self._is_cancelled_fn(): raise RuntimeError("测试已被用户取消")`
3. `MobileBlueprintRunner.run()` 注入回调：`self._ctrl._is_cancelled_fn = lambda: self._test_controller.is_cancelled`

**变更文件清单**：

| 文件 | 变更内容 |
|------|---------|
| `src/testing/mobile_blueprint_runner.py` | +test_controller参数、+APP启动逻辑、+生命周期管理、+取消中断 |
| `src/controller/android.py` | +_is_cancelled_fn属性、+循环内取消检查 |
| `src/api/routes.py` | 两个Android蓝本端点都传入 test_controller |

**铁律**：
- ✅ `MobileBlueprintRunner` 必须调用 `test_controller.start()` — 否则停止/暂停永远不生效
- ✅ 测试前必须用 `adb am start` 确保 APP 在前台 — 不能依赖 `create_mobile_session` 阶段的启动
- ✅ 所有重试循环（`_find_element_with_wait` 等）必须包含取消信号检查
- ⚠️ 插件侧 `createMobileSession` 只传 `device_name` 是已知限制，引擎侧需从蓝本补偿

---

## 十二、Windows桌面测试增强（2026-03-12）

> **最终成果：97%通过率（33/34步骤），5分钟完成全部测试**
> 被测应用：NoteApp（tkinter桌面应用，含登录/笔记CRUD/搜索/登出功能）

### 12.1 本次改动概览

| 改动 | 文件 | 效果 |
|------|------|------|
| 截图BMP→PNG | `window_manager.py` | 6KB vs 875KB，100倍压缩 |
| pywinauto替换SendInput | `desktop.py` | 稳定前台输入，tkinter兼容 |
| client区域截图（核心） | `window_manager.py` + `desktop.py` | 去掉标题栏边框，AI坐标精确对齐 |
| AI OCR fallback | `desktop_blueprint_runner.py` | tkinter UI树不暴露文本时，AI视觉识别 |
| reasoning_effort分级 | `desktop_blueprint_runner.py` | 坐标定位`low`(15s)，OCR`minimal`(2s) |
| app_exe自动重启 | `routes.py` | 测试前WM_CLOSE关旧窗口+启动新实例 |
| 蓝本状态管理 | `desktop-demo/testpilot.json` | 场景间加Logout重置状态 |

### 12.2 踩坑记录（按时间顺序）

#### 坑16：SendInput对tkinter不稳定 → pywinauto替代

- **现象**：`SendInput`模拟鼠标/键盘是全局前台操作，对tkinter有时点击无响应，且抢占用户鼠标
- **试错**：先试`PostMessage`后台模式→tkinter不响应WM_LBUTTONDOWN/WM_CHAR；再试SendInput前台→不稳定且干扰用户
- **方案**：`pywinauto` Application(backend='win32').connect(handle=hwnd)，click_input+type_keys完美兼容tkinter
- **文件**：`desktop.py` — `__init__`新增`_pwa_win`，`launch`中连接，`_click_at`/`_send_text_bg`用pywinauto
- **教训**：tkinter等非标准Win32框架，PostMessage无效，SendInput不稳定，pywinauto最可靠

#### 坑17：截图BMP 875KB太大 → PNG 6KB

- **问题**：`PrintWindow`+`GetDIBits`输出原始BMP，上传AI分析时base64编码后更大
- **方案**：用Pillow将BGRA原始数据转PNG，fallback到BMP
- **文件**：`window_manager.py` — 新增`_save_as_png()`，`capture_window`/`capture_screen`改为输出PNG
- **效果**：875KB → 6KB，100倍压缩

#### 坑18：AI坐标偏移——截图含标题栏（最关键！）

- **现象**：AI返回边缘坐标(x=0.97/y=1.0)时，点击超出窗口范围。Logout按钮y=1.0点到窗口最底部，Add按钮x=0.97超出宽度
- **根因**：`GetWindowRect`+`PrintWindow`截的是完整窗口(416x539含标题栏+边框)，AI坐标基于此计算，但client区域只有400x500。边缘元素坐标本就接近1.0，加上标题栏占比偏移后更易超界
- **试错1**：改`reasoning_effort`从`medium`到`minimal` → 速度快但坐标更不准(y=1.0)
- **试错2**：`clamp(0.03, 0.97)`硬编码限制 → **hack！不同应用/窗口不通用，不专业**
- **正确方案**：截图只截client区域，坐标计算也用client区域。截图和坐标基准完全一致，任何窗口都适用
- **实现**：
  - `window_manager.py` 新增 `get_client_rect()` — `GetClientRect`+`ClientToScreen`获取client屏幕坐标
  - `capture_window()` — 先`PrintWindow`全窗口，再`BitBlt`裁剪client区域
  - `desktop.py` `launch()` — 存储`client_rect`而非`window_rect`
- **坐标链路**：AI看client截图(400x500) → 归一化坐标(0.91) → `client_left + 0.91*400` = 屏幕绝对坐标 → pywinauto: `abs_x - window_left` = 窗口相对坐标（自动包含边框偏移）

#### 坑19：tkinter UI Automation树几乎为空

- **现象**：tkinter的UI Automation树只暴露窗口标题"NoteApp"，内部Entry/Button/Listbox的Name/AutomationId全为空
- **影响**：`get_text()`/`get_visible_text()`只返回"NoteApp"，所有`assert_text`步骤失败
- **方案**：AI视觉OCR fallback——截图发给AI识别所有可见文字
- **文件**：`desktop_blueprint_runner.py` `_get_text()` — 三级降级：UI精确→UI全窗口→AI OCR
- **OCR prompt**：`"请识别这张截图中所有可见的文字内容，逐行列出。只输出文字内容本身。"`
- **效果**：assert_text通过率从0%提升到接近100%

#### 坑20：AI视觉分析极慢（3-4分钟/次）

- **现象**：桌面测试AI调用每次3-4分钟，34步测试需20+分钟。但手动发API只要5-8秒
- **根因**：默认`reasoning_effort="medium"`，视觉分析（图片+复杂提示词）在medium下深度推理
- **方案**：分级设置——坐标定位用`low`(~15s)，OCR用`minimal`(~2s)。不用minimal做坐标（精度不够），不用medium（太慢）
- **文件**：`desktop_blueprint_runner.py` 三处`analyze_screenshot`调用
- **效果**：20+分钟 → 5分钟

#### 坑21：蓝本场景间状态不重置

- **现象**：登录成功后停在主页，后续"空用户名登录"场景找不到登录页元素（Username输入框、Login按钮）
- **方案**：重构蓝本，场景顺序改为「正确登录→主页操作→Logout→错误登录场景」，每段需要登录页的场景前确保先登出
- **文件**：`desktop-demo/testpilot.json` — 7个场景按状态依赖排序

#### 坑22：测试前NoteApp残留在主页

- **现象**：上次测试结束后NoteApp停在主页（没logout成功），新测试开始时AI看到主页而不是登录页
- **方案**：API新增`app_exe`参数，测试前自动`FindWindow`+`WM_CLOSE`关旧窗口，再`subprocess.Popen`启动新实例
- **文件**：`routes.py` desktop-blueprint端点
- **用法**：`{"app_exe": "python D:\\Projects\\TestPilotAI\\desktop-demo\\notepad_app.py"}`

### 12.3 坐标系统设计总结（通用方案）

```
截图范围 = client区域（GetClientRect + ClientToScreen）
         不含标题栏、不含窗口边框
         AI看到什么就点什么，零偏移

坐标计算 = client_left + nx * client_width  →  屏幕绝对坐标
         pywinauto自动转窗口相对坐标

适用范围 = 任何Windows桌面应用
         任何窗口大小和位置
         不需要硬编码clamp或offset
```

### 12.4 变更文件清单

| 文件 | 变更 |
|------|------|
| `src/controller/window_manager.py` | +`get_client_rect()`, `capture_window`改为截client区域+PNG输出 |
| `src/controller/desktop.py` | +pywinauto连接, `launch`存client_rect, `_click_at`/`_send_text_bg`用pywinauto |
| `src/testing/desktop_blueprint_runner.py` | +AI OCR fallback, +reasoning_effort分级, +提示词优化 |
| `src/api/routes.py` | +`app_exe`自动重启, desktop-blueprint端点增强 |
| `src/core/ai_client.py` | +BMP MIME类型支持 |
| `desktop-demo/testpilot.json` | 蓝本重构：7场景34步，状态连贯 |

### 12.5 待修复（晚上继续）

- **步骤28**：空用户名场景Login按钮定位错误。Logout后AI预分析截图时页面可能还没完全切回登录页，把主页的Logout位置误当Login。需增加页面切换等待或状态检测
- **清理测试文件**：`desktop-demo/test_pywinauto.py`、`desktop-demo/test_postmessage.py` 调试用脚本可删除

---

## 十三、桌面测试调试（家里环境）（2026-03-13）

### 13.1 核心Bug：pywinauto未安装导致输入完全失败

**现象**：桌面测试登录步骤只输入了`a`+乱码（2个字符），`admin`/`admin123`完全没输进去，登录失败导致后续26步连锁失败（26/34通过但全是非登录相关步骤凑的）

**排查过程**（走了弯路）：
1. ❌ 以为是type_keys速度太快 → 加pause=0.05 → 无效
2. ❌ 以为是坐标偏移（标题栏31px） → 改用SendInput绝对坐标 → 无效
3. ❌ 以为是焦点被Windsurf抢走 → 窗口置顶方案 → 方向错误（用户没动鼠标）
4. ✅ **真正根因**：家里电脑`poetry install`时pywinauto没装上（pyproject.toml有但实际未安装），`desktop.py`的`launch()`中`import pywinauto`走了`except`分支，`_pwa_win=None`，所有操作走SendInput fallback

**备忘录坑16已明确记录**："SendInput对tkinter不稳定→pywinauto替代"。但因为pywinauto没装，代码静默降级到了SendInput。

**修复**：
```bash
poetry install  # 安装pywinauto 0.6.9 + comtypes + pillow + psutil
```

**验证**：安装后登录成功，OCR看到`Total: 3 notes`、添加笔记`Test Note 123`成功、搜索Hello成功。

### 13.2 坐标计算确认（正确，无需修改）

| 项目 | 值 |
|------|-----|
| 窗口rect | left=156, top=156 |
| client origin | x=164, y=187 |
| 标题栏高度 | 31px |
| 边框宽度 | 8px |

`_click_at`坐标公式 `rel = screen_abs - window_rect.left` 是正确的：
- screen_abs = client_left + norm * client_width = 164 + 200 = 364
- rel = 364 - 156 = 208
- pywinauto实际点击 = 156 + 208 = 364 ✅

### 13.3 发现的新问题

1. **AI API超时**：AI预分析截图分析请求偶尔超时（4分钟无响应，`Request timed out`），导致测试整体很慢。需要：
   - 加超时重试机制（如30秒超时+重试1次）
   - 或降低reasoning_effort进一步减少AI处理时间

2. **桌面应用自动启动**：目前`app_exe`通过API参数传入，应写入蓝本JSON让用户更方便

3. **截图策略**：桌面测试的3类截图（AI预分析、OCR识别、视觉定位）是必需的，已有hash缓存避免重复AI调用

### 13.4 铁律

- ✅ **新环境必须先`poetry install`确认所有依赖已安装**，pywinauto缺失会静默降级到SendInput导致tkinter输入完全失败
- ✅ `desktop.py`的pywinauto导入是try/except静默降级，不会报错，只能从日志`pywinauto 连接失败`或`pywinauto 已连接`判断
- ✅ 桌面测试坐标：AI返回归一化坐标→乘以client尺寸→加client origin→得到屏幕绝对坐标→减window rect得到click_input相对坐标

### 13.5 截图逻辑验证结果

`desktop_blueprint_runner.py`第343行的截图逻辑是正确的：
```python
need_screenshot = (step_def.action == "screenshot") or (step_def.expected and self._ai)
```
只在蓝本写了`screenshot`动作或有`expected`需要AI验证时才截图，不会每步都截。桌面测试额外的截图（AI预分析、OCR、视觉定位）是AI识别所必需的，不是冗余截图。

### 13.6 AI API超时修复（2026-03-14早）

**问题**：AI预分析/单元素定位调用，`request_timeout=60s × max_retries=3 = 最长180秒`等待，方舟平台偶尔不响应时测试严重卡顿

**修复**：
- `ai_client.py`：`_call_chat` 接收 `timeout` 参数，传入时用 `client.with_options(max_retries=0)` 创建不重试的临时客户端
- `desktop_blueprint_runner.py`：AI预分析和单元素定位 `timeout=30`，OCR `timeout=20`
- 效果：超时从2-4分钟→精确30秒

**验证**：日志确认 09:28:31→09:29:01 正好30秒超时

### 13.7 蓝本自动启动应用（2026-03-14早）

**蓝本新增字段**：
```json
{
  "window_title": "NoteApp",
  "app_exe": "python notepad_app.py"
}
```

**引擎行为**（`routes.py`）：
1. 从蓝本JSON读取 `app_exe` 和 `window_title`（API参数可覆盖）
2. 有旧窗口→发送 WM_CLOSE→等关闭→在蓝本所在目录启动 `app_exe`
3. 无旧窗口→直接启动
4. 等3秒让应用完成初始化

### 13.8 测试结果对比

| 测试 | 通过 | 失败原因 | 总耗时 |
|------|------|----------|--------|
| 3/13晚（修复前） | 28/34跑到中途断开 | pywinauto没装+API超时4分钟 | >11分钟未完 |
| 3/14第1次 | 31/34 | 步骤9 AI超时(30s→OK)、步骤12连锁、步骤28断言 | 11分钟 |
| 3/14第2次 | 28/34 | 方舟API大面积超时（非代码问题） | 6分钟 |

**结论**：初步修复后有改善，但仍有超时。真正根因见13.9。

### 13.9 【重大】AI API超时真正根因（2026-03-14上午）

**现象**：桌面测试AI调用频繁超时（30秒），但Web/Android/小程序测试从不超时，手动调用API 7秒就返回。

**排查过程**：
1. 手动测试API → 7秒返回，排除方舟平台问题
2. 对比不同 `max_completion_tokens` 的耗时：

| max_completion_tokens | 耗时 | 输出tokens | reasoning_tokens | 结果 |
|---|---|---|---|---|
| 65535（全局默认） | 31.9s | 1245 | **1185** | 超时边缘 |
| 4096 | 60s | - | - | 超时 |
| **1024** | **6.0s** | 211 | 152 | ✅ 正常 |
| 256 | 8.4s | 256 | 256 | 响应被截断 |

**根因**：`max_completion_tokens=65535`（全局配置）导致模型**过度思考（reasoning）**！
- 一个简单的坐标定位请求，模型用了1185个reasoning tokens（本该只需要150个）
- reasoning tokens占了93%的输出，实际有效输出只有60 tokens
- Web/Android测试不受影响：它们的prompt场景不同，模型不会过度思考

**修复**：
- `ai_client.py`：`_call_chat` 和 `analyze_screenshot` 新增 `max_tokens` 参数，允许调用方覆盖全局值
- `desktop_blueprint_runner.py`：
  - AI预分析（多元素坐标）：`max_tokens=1024`
  - AI单元素定位：`max_tokens=1024`（512太小，reasoning占满后无实际输出）
  - AI OCR文字识别：`max_tokens=2048`
- `max_retries` 从 `0` 改为 `1`：避免瞬时 `Connection error` 直接失败

**效果**：
- 单次AI调用：32秒 → **6秒**（5倍提升）
- 全量测试：11分钟 → **3分钟**（3.7倍提升）
- 超时率：~30% → **0%**（正常网络下）

**经验教训（铁律）**：
1. **reasoning模型的max_completion_tokens包含reasoning tokens**，设太大会导致模型过度思考
2. **不同场景必须设不同的max_tokens**：简单JSON返回用1024，文字识别用2048，复杂分析才用65535
3. **max_retries=0会让Connection error直接失败**，至少设1次重试
4. **排查API慢时先手动测试排除平台问题**，再对比参数差异

### 13.11 _get_text文本获取优先级调整

**问题**：PyQt5应用的UI Automation暴露了按钮Name属性（Add/Delete/Toggle Done），`_get_text`认为是"有意义的文本"直接返回，跳过了AI OCR。导致`assert_text`断言失败（UI文本不包含列表内容和统计信息）。

**修复**：`_get_text` 优先级改为 AI OCR优先 → UI Automation降级
- AI OCR能识别所有可见文字（包括列表内容、统计、图标文字等）
- UI Automation只能获取控件Name属性，信息不完整
- 加了 `or ""` 防止返回None导致 `TypeError: expected string or bytes-like object`

### 13.12 桌面蓝本规范文档

新建 `docs/desktop-blueprint-spec.md`，规范内容包括：
- 蓝本JSON结构和必填/可选字段
- 启动策略（自动启动 `app_exe` / 手动启动）
- 步骤动作（click/fill/screenshot/assert_text/wait）
- target写法和定位优先级（UI Automation → AI视觉）
- 场景设计原则（状态连贯、独立断言、页面切换加wait）
- 适用框架对照表

### 13.13 PyQt5 Todo应用测试（新demo验证）

**目的**：验证桌面测试对非tkinter框架的通用性

**应用**：`todo-demo/todo_app.py`（PyQt5，待办事项应用）
- 功能：添加/标记完成/删除/清空已完成/统计
- 窗口标题：TodoApp
- 有AccessibleName属性（PyQt5支持UI Automation）

**测试结果**（修复后最终跑）：
- 通过率：**22/27**
- 总耗时：**3分钟**
- 失败原因：Toggle Done前没选中列表项（点击位置不够精确，QListWidget需要精确点击item区域）
- 这是蓝本和应用交互的精度问题，不是引擎Bug

### 13.14 已知问题（更新）

1. **NoteApp步骤28**：空用户名登录提示为 `Please enter password` 而非 `Please enter username`（可能是App Bug）
2. **QListWidget点击精度**：AI视觉定位点击列表项可能不够精确，需要改进列表项的定位策略
3. **方舟API偶发抖动**：已通过 `max_retries=1` 缓解，正常网络下不再超时

### 13.15 测试结果对比（完整版）

| 测试 | 应用 | 通过 | 失败原因 | 总耗时 |
|------|------|------|----------|--------|
| 3/13晚 | NoteApp | 28/34中途断 | pywinauto没装+API超时4分钟 | >11分钟 |
| 3/14早1 | NoteApp | 31/34 | API超时30s+连锁+断言 | 11分钟 |
| 3/14早2 | NoteApp | 28/34 | API大面积超时（max_tokens=65535） | 6分钟 |
| 3/14上午（修复后） | TodoApp | **22/27** | Toggle列表项未选中（蓝本交互） | **3分钟** |
| 3/14上午（蓝本规范后） | TodoApp | **25/27** | Buy milk点击未选中列表项（AI定位精度） | **2分39秒** |

**速度提升**：11分钟 → 2分39秒（4倍+），单次AI调用32秒→6秒（5倍）

### 13.16 【重要】蓝本target写法规范（2026-03-14上午）

**问题**：Todo测试步骤10/11反复失败（AI找不到Toggle Done按钮），但按钮明明在屏幕上。

**根因**：蓝本target加了中文后缀！
- `"target": "name:Toggle Done按钮"` → 屏幕上只有 `Toggle Done`，AI搜索"Toggle Done按钮"匹配不上
- `"target": "name:Buy milk列表项"` → 屏幕上只有 `○ Buy milk`，AI搜索"Buy milk列表项"匹配不上

**铁律**：`target` 的 `name:` 后面必须是**屏幕上肉眼可见的原文**，不加任何中文后缀（按钮/输入框/列表项等）。元素类型描述放在 `description` 里。

```
✅ "target": "name:Toggle Done", "description": "列表下方左侧的绿色Toggle Done按钮"
❌ "target": "name:Toggle Done按钮"
```

**修复后效果**：22/27 → **25/27**，剩余2个失败是Buy milk点击后QListWidget未选中（AI坐标落在item间隙，引擎精度问题）

**已更新**：`docs/desktop-blueprint-spec.md` 第六节完整重写target规范

### 13.17 全局蓝本编写规范 Workflow（2026-03-14上午）

**目标**：让任何AI编程助手（Windsurf/Cursor等）生成蓝本时自动遵守规范，不再出现target加中文后缀等低级错误。

**文件**：`.windsurf/workflows/blueprint.md`（Windsurf会自动读取）

**规范内容**：
1. **target铁律**：`name:`后必须是屏幕原文，不加"按钮/输入框/列表项"等中文后缀
2. **description最佳实践**：位置+颜色+相邻元素+元素类型
3. **平台target格式差异**：desktop用`name:`屏幕原文，web/小程序用CSS选择器，android用`accessibility_id:`
4. **wait时长规范**：页面切换≥1500ms，数据刷新500-1000ms，弹窗500ms
5. **断言写法**：expected必须是界面实际显示的精确文本片段
6. **自查清单**：7项必检项，生成蓝本后逐项验证

**验证**：
- NoteApp蓝本规范修复后：28/34通过，7分19秒
- 失败步骤集中在Logout后wait=1000ms不够（已改为2000ms）
- target定位问题全部消除

**盲测验证**（假装不了解项目，只读workflow规范后从零写蓝本）：
- TodoApp：25/28通过，3个失败全是AI预分析偶发不返回Buy milk坐标（引擎问题，非蓝本问题）
- 同样写法的Read book（步骤16）0.6s成功，证明蓝本写法完全正确

### 13.18 待做：窗口锁定位置（用户建议）

**用户想法**：把被测应用窗口固定在屏幕特定区域（如1/4屏），防止被其他应用遮挡、窗口位置漂移导致坐标偏移。

**可行性分析**：
1. **固定窗口位置** — 完全可行。每次操作前 `set_focus()` + `move_window()` 锁定位置
2. **窗口内部坐标** — 已经在做。截图是客户区截图，AI返回百分比坐标再换算
3. **虚拟触点网格**（AI视觉+UIA合并成完整控件树缓存）— 好方向，工作量大，后续版本

**实现优先级**：第1点（锁定窗口）可以马上加到引擎里，解决窗口被遮挡和位置漂移问题

### 13.19 AI定位不稳定根因分析与修复（2026-03-14）

#### 问题现象
桌面蓝本测试25/28通过，3个失败全是AI预分析偶发不返回坐标（如Buy milk），即使蓝本target写法完全正确。

#### 根因分析：各平台AI调用方式对比

| 平台 | AI任务 | reasoning_effort | max_tokens | 为什么不超时 |
|------|--------|-----------------|------------|------------|
| Web | 只做**验证**（截图是否符合预期） | low | 不限制 | 有完整DOM树，CSS选择器直接操作 |
| 安卓 | 只做**验证** | low | 不限制 | 有accessibility树，ID直接操作 |
| 小程序 | 只做**验证** | low | 不限制 | 有WXML树，CSS选择器直接操作 |
| 桌面 | **定位+验证**（看截图找坐标） | low | ~~1024~~ → 不限制 | 无控件树，AI必须看图定位 |

**核心差异**：Web/安卓/小程序的AI**从不需要定位元素**，都通过选择器/ID精确操作。只有桌面AI需要"看图找坐标"，prompt更复杂，reasoning消耗更大。

之前给桌面限制max_tokens=1024是错误的——reasoning吃满token后实际输出为空。改成不限制后跟其他平台一致，AI可以自由思考后输出准确坐标。

#### 修复内容

1. **去掉max_tokens限制**（`desktop_blueprint_runner.py`）— 预分析和单元素定位都不限制，跟Web/安卓一致
2. **坐标容错 `_normalize_coord`**（`desktop_blueprint_runner.py`）— 兼容三种格式：
   - 标准归一化: `0.5` → `0.5`
   - 绝对像素: `219` → `219/dimension`
   - 字符串分数: `"219/889"` → `0.246`
3. **修复venv python启动**（`routes.py`）— `app_exe="python todo_app.py"` 自动检测虚拟环境python，解决系统python缺PyQt5
4. **窗口轮询等待**（`routes.py`）— 从固定3秒改为轮询最多10秒等窗口出现
5. **蓝本规范更新**（`blueprint.md`）— description新增"预期页面变化"要求，编程AI根据源码预测功能行为

#### 修复后测试结果

**26/28通过**，2个失败原因均非AI定位问题：
- Connection error（网络抖动）→ 偶发，非代码问题
- OCR返回不完整（只返回标题"Todo App"）→ OCR质量问题

**AI定位全部正常** — 返回标准归一化坐标如 `{"x": 0.891, "y": 0.131}`，无格式异常。

#### 铁律补充

1. **桌面AI调用不要限制max_tokens** — 跟Web/安卓/小程序一致，让模型自由输出
2. **reasoning_effort="low"是正确选择** — 不思考(minimal)会格式乱，过度思考(medium/high)会慢
3. **坐标容错必须保留** — AI模型升级后输出格式可能变化，容错是防线
4. **app_exe用虚拟环境python** — 系统python通常没有项目依赖
5. **弹窗后必须关闭** — 任何触发弹窗的操作后，蓝本必须加click OK/Yes步骤关闭弹窗，否则遮挡主界面导致后续连锁失败
6. **OCR用low不用minimal** — minimal对复杂界面只读标题，low能识别全部文字

### 13.20 增强版TodoApp压力测试（2026-03-14）

#### 新功能
在原3功能基础上新增7个功能，大幅提高UI复杂度：
- 优先级标记（High/Medium/Low，彩色[H]/[M]/[L]标签）
- 搜索过滤（实时过滤+过滤状态提示"Showing X of Y items"）
- 排序（按名称/优先级/状态）
- 编辑（双击/Edit按钮，弹出QInputDialog）
- 删除确认弹窗（QMessageBox.question）
- 重复检测（不区分大小写）
- 全选/取消全选（Select All按钮）
- 统计栏新增Urgent计数（未完成的High优先级数）
- 初始数据5条（原3条）

#### 蓝本
10个场景、58步（原6场景28步），覆盖所有新功能。

#### 测试结果演进
| 轮次 | 结果 | 主要问题 |
|------|------|---------|
| 第1轮 | 48/54 (89%) | 弹窗未关闭→后续连锁失败 |
| 第2轮 | 53/58 (91%) | 蓝本加弹窗关闭步骤后改善，剩余失败=OCR质量+API超时 |
| 第3轮 | 待验证 | OCR从minimal改为low |

#### 发现的问题
1. **弹窗遮挡问题**（已修复蓝本）：编程AI写蓝本时遗漏了弹窗关闭步骤，弹窗遮挡主界面导致后续所有操作失败
2. **OCR质量问题**（已修复引擎）：`reasoning_effort="minimal"`对复杂页面只读出标题"Todo App"，改为`"low"`后应能识别全部文字
3. **API偶发超时**：30秒timeout内未响应，网络层面问题

### 13.21 ⭐ 重要下一步：弹窗自愈机制（三层防御）

**问题本质**：蓝本是编程AI写的，即使规范写得再好，AI也可能遗漏弹窗关闭步骤。引擎必须有自救能力，不能被弹窗卡死。

**当前行为**：步骤找不到目标 → 标记Bug → 继续下一步 → 下一步也被弹窗挡 → 连锁失败，整个场景废掉。

**目标行为**：步骤失败 → 检测弹窗 → 自动关闭 → 重试原步骤 → 如果还失败就标记Bug跳到下一场景。

#### 三层防御设计

| 层 | 策略 | 实现方式 |
|---|------|---------|
| **第1层：预防** | 蓝本规范（已做） | workflow要求弹窗后加关闭步骤 |
| **第2层：检测+自愈** | 步骤失败时截图分析 | AI看截图判断"是否有弹窗？OK/Cancel按钮在哪？"，有就自动点击关闭，然后重试原步骤 |
| **第3层：止损** | 场景级跳过 | 自愈失败时，标记当前场景为Bug，跳到下一个场景，不被卡死 |

#### 第2层实现要点
```
步骤执行失败（点击目标找不到/超时）
  → 截图当前界面
  → AI分析："截图中是否有模态弹窗/对话框？如果有，返回关闭按钮(OK/Cancel/X)的坐标"
  → 如果检测到弹窗：
    → 点击关闭按钮
    → 等待500ms
    → 重试原步骤
    → 把"意外弹窗"作为Bug信息记录到报告
  → 如果没有弹窗：
    → 正常走Bug标记流程
```

#### 第3层实现要点
```
当前场景连续失败N步（如3步）
  → 认为场景已不可恢复
  → 标记整个场景为"blocked"
  → 跳到下一个场景的第一步
  → 避免在废掉的场景里浪费AI调用
```

**优先级**：第2层（弹窗自愈）最重要，能解决90%的卡死问题。第3层（场景跳过）是兜底。

### 13.22 弹窗自愈+蓝本拆分实施（2026-03-10）

#### 13.22.1 弹窗缓存跳过修复验证

**问题**：`_smart_tap` 第1层预分析缓存为对话框按钮（OK/Yes/No/Cancel）保存旧坐标，弹窗出现后坐标失效。

**修复内容**：
- `desktop_blueprint_runner.py`：已有 `_DIALOG_BUTTONS` 集合 + `is_dialog_btn` 跳过逻辑
- `mobile_blueprint_runner.py`：本次新增同样的 `_DIALOG_BUTTONS` 和跳过逻辑

**验证结果**（原始单文件蓝本）：52/58 → 53/58，弹窗点击步骤全部通过。

#### 13.22.2 弹窗自愈机制实现（三层防御）

在 `desktop_blueprint_runner.py` 中实现完整三层防御：

**第2层 `_try_heal_popup()`**：
- 步骤失败时（click/fill/assert_text/assert_visible），自动截图发AI
- AI判断截图中是否有模态弹窗，并返回关闭按钮坐标
- 有弹窗 → 点击关闭 → 清空缓存 → 重试原步骤
- 无弹窗 → 正常走Bug标记流程

**第3层 场景级跳过**：
- 在 `run()` 循环中追踪 `_consecutive_failures` 计数
- 连续失败 ≥ 3步 → 标记剩余步骤为 `[跳过-场景blocked]` → break到下一场景
- 场景切换时重置计数

**代码位置**：
- `_try_heal_popup()` 方法：在 `_smart_tap()` 之前
- 自愈逻辑：`run()` 循环中 `_execute_step` 之后
- 连续失败跳过：同一位置，检查 `_consecutive_failures >= 3`

#### 13.22.3 TodoApp蓝本拆分为独立模块

**原因**：单文件58步场景间状态连贯，一个失败后续连锁崩溃。拆分后每个蓝本独立启动应用，互不干扰。

**拆分结果**：
```
todo-demo/testpilot/
  init.testpilot.json         ← 初始状态验证（6步）
  add_priority.testpilot.json ← 添加+优先级（9步）
  search.testpilot.json       ← 搜索过滤（8步）
  toggle_sort.testpilot.json  ← 标记完成+排序（10步）
  delete.testpilot.json       ← 删除含弹窗确认（8步）
  edge_cases.testpilot.json   ← 重复检测+全选+清空+空输入（21步）
```

**关键设计**：每个蓝本都有 `app_exe: "python todo_app.py"`，引擎每次启动全新的TodoApp实例。

#### 13.22.4 testpilot子目录工作目录修复

**发现问题**：蓝本放在 `testpilot/` 子目录后，引擎以 `bp_file.parent`（即 `testpilot/`）作为 `cwd` 启动应用，导致 `python todo_app.py` 找不到文件。

**修复**：在 `routes.py` 的 `run_desktop_blueprint_test` 中添加：
```python
if bp_dir.name == "testpilot":
    bp_dir = bp_dir.parent
```

#### 13.22.5 拆分后测试结果

| 蓝本 | 结果 | 步骤 | 耗时 |
|------|------|------|------|
| init | 100% | 6/6 | 24.8s |
| search | 100% | 8/8 | 47.4s |
| toggle_sort | 100% | 10/10 | 33.7s |
| delete | 100% | 8/8 | 123.3s |
| edge_cases | 100% | 21/21 | 191.9s |
| add_priority | 89% | 8/9 | 21.9s |
| **总计** | **98.4%** | **61/62** | **443s** |

**对比**：原单文件蓝本 53/58 (91%) → 拆分后 61/62 (98.4%)！

**唯一失败**：`add_priority` 步骤8（Urgent:3断言），原因是 `name:High` 对QComboBox下拉框的交互不精确，实际添加的仍是Medium优先级。这是引擎与QComboBox控件的交互问题，与弹窗无关。

#### 13.22.6 经验总结

1. **蓝本拆分效果显著**：独立蓝本消除了场景间状态依赖，失败不会连锁传染
2. **弹窗缓存跳过有效**：对话框按钮（OK/Yes/No）必须跳过预分析缓存，强制实时定位
3. **testpilot/子目录工作目录**：蓝本规范要求放`testpilot/`子目录，引擎必须用父目录作为cwd
4. **QComboBox交互待改进**：`name:High` 点击只是匹配了控件Name属性，但QComboBox需要先展开下拉→再点击选项，当前引擎不支持这种两步交互

### 13.23 AI中枢决策层接入（2026-03-14）

#### 13.23.1 架构设计

创建 `src/testing/ai_hub.py` 作为跨平台统一的AI大脑，所有Runner步骤失败时统一向Hub请求决策。

**分层策略：**
| 层级 | 能力 | 触发条件 | AI成本 |
|------|------|----------|--------|
| L0 | 直通 | 步骤通过 | 0 |
| L1 | 弹窗自愈 | 步骤失败 | ~¥0.01/次 |
| L2 | 失败诊断（预留） | L1未解决 | ~¥0.02/次 |
| L3 | 场景级熔断 | 连续失败≥3 | 0 |

**核心类：**
- `AIHub` - 中枢决策器，持有AI客户端，提供 `on_step_failed()`/`on_step_passed()`/`on_scenario_start()`
- `StepContext` - 步骤上下文数据包，包含 `screenshot_fn`/`click_fn` 平台回调
- `HubDecision` - 决策结果（RETRY/SKIP_STEP/SKIP_SCENE/NONE）
- `DIALOG_BUTTON_KEYWORDS` - 跨平台弹窗按钮关键词集合

#### 13.23.2 三个Runner统一接入

**改造方式：** 每个Runner在`__init__`中创建`self._hub = AIHub(ai_client)`，在步骤循环中：
- 步骤通过 → `hub.on_step_passed()`
- 步骤失败 → 构造`StepContext`（注入平台特定的截图/点击回调）→ `hub.on_step_failed(ctx)` → 根据返回的`HubAction`执行重试/跳过

**平台桥接方法：**
- Desktop: `_hub_screenshot()` → `ctrl.screenshot()`, `_hub_click(nx,ny)` → 窗口坐标转换 → `ctrl._click_at()`
- Web: `_hub_screenshot()` → `browser.screenshot()`, `_hub_click(nx,ny)` → viewport乘法 → `page.mouse.click()`
- Mobile: `_hub_screenshot()` → `ctrl.screenshot()`, `_hub_click(nx,ny)` → 屏幕尺寸乘法 → `ctrl.tap_xy()`

**关键变化：**
- Desktop Runner的`_try_heal_popup()`方法已删除，逻辑迁移到AIHub._try_l1_popup_heal()
- 三个Runner的`_DIALOG_BUTTONS`统一引用`DIALOG_BUTTON_KEYWORDS`
- 三个Runner的`is_dialog_btn`判断统一使用`AIHub.is_dialog_button()`
- 连续失败计数/熔断逻辑全部由Hub管理

#### 13.23.3 验证结果

- `ai_hub.py` 导入测试：✅ OK
- `desktop_blueprint_runner.py` 导入测试：✅ OK
- `blueprint_runner.py` (Web) 导入测试：✅ OK
- `mobile_blueprint_runner.py` 导入测试：✅ OK
- 引擎启动（端口8900）：✅ 正常，无报错
- API健康检查：✅ 200 OK

#### 13.23.4 L2升级：截图+步骤历史直接问AI（v12.1, 2026-03-14）

**核心改进：** L2不再只看当前步骤，而是把最近8步操作历史一起发给AI，像"坐在你旁边的同事看图说话"。

**新增类/字段：**
- `StepRecord` dataclass：step_num/action/target/value/passed/error + `describe()` 方法（自然语言描述）
- `AIHub._step_history`：最近8步操作记录列表
- `AIHub.record_step()`：Runner每步执行完都调用
- `AIHub._build_history_context()`：拼成大白话历史供L2分析
- `AIHub.on_scenario_start()`：场景开始时清空历史

**L2提示词核心改变：** 把步骤历史+当前失败+截图三合一发给多模态AI，AI像人一样观察分析。

**三Runner改造：**
- 每步执行后统一调用 `hub.record_step(step_num, action, target, value, passed, error)`
- SKIP_STEP 处理：三个Runner统一支持

**验证（故意打破蓝本测试L2）：**
- 改坏 `add_priority.testpilot.json`：选择器写错 + 断言不可能通过
- L2成功诊断："添加的High优先级任务实际显示为Medium优先级"（AI看截图看到了`[M] Fix critical bug`）
- 结果：7/9通过，2个Bug正确识别，L1调2次，L2调2次

**发现的问题：**
- JSON中文引号导致`json.loads`失败（`"xxx"` → 需替换为`"xxx"`）
- 大截图(393KB)可能超AI超时(25秒)

#### 13.23.5 归因预判：谁的锅？（v12.2, 2026-03-14）

**问题：** 所有失败都调AI分析 → 浪费token。但"文本不匹配"这种100%是应用Bug，不需要AI看。

**L0.5规则预判（零token）：**
```
步骤失败 → AIHub.on_step_failed()
  → L3 熔断（无AI）
  → L0.5 规则预判（无AI）
       ├─ "文本断言失败: 预期'X', 实际'Y'" → APP Bug，不调AI ✅
       ├─ "计算验证失败" → APP Bug，不调AI ✅
       └─ 其他 → 继续L1/L2（调AI）
  → L1 弹窗自愈（AI截图）
  → L2 失败诊断+归因（AI截图+历史）
```

**新增 `FaultType` 枚举：**
- `APP` — 应用的Bug（数据错误/崩溃/功能异常）→ 报给编程AI
- `TEST` — 测试脚本/框架问题（选择器错/时序/弹窗）→ 中枢自己处理
- `UNKNOWN` — 无法判断

**Bug标签系统：** Runner根据 `decision.fault` 给Bug.category加前缀：
- `[应用Bug]文本断言失败` → 编程AI该修
- `[测试问题]步骤执行错误` → 我们的问题，编程AI不用管

**L2提示词升级：** 要求AI同时判断fault归因 + 中文引号容错修复

**统计新增：** `hub.stats["rule_judgments"]` — 规则预判次数（省了多少次AI调用）

---

## 十四、v13.0 社区经验库 + 商业化（2026-03-15）

> **双机并行策略**：Windows 做社区/商业化（v13.0-A/B/C/D），Mac 做平台支持（v13.0-E）。
> **全部在同一天完成 A→B→C→D 四个子阶段。**

### 14.1 v13.0-A 数据库 + 后端 API ✅

**新增 6 张表**（`src/community/models.py`）：
- `shared_experiences` — 匿名化后的调试经验
- `experience_votes` — 投票/采纳/举报记录
- `user_badges` — 10 种成就勋章
- `user_profiles` — 用户公开资料
- `debug_snapshots` — 私有调试快照
- `api_keys` — 第三方 API 密钥
- `credit_transactions` — 积分流水

**23 个 API 端点**：
- 社区（`src/community/routes.py`）：经验 CRUD + 搜索推荐 + 投票 + 排行榜 + 快照 + 积分
- 计费（`src/billing/routes.py`）：套餐查询 + 积分余额 + 用量流水 + Webhook

**其他**：Alembic 迁移配置 + psycopg2-binary 驱动 + SQLAlchemy 双驱动（SQLite/PostgreSQL）

### 14.2 v13.0-B 社区 Web 门户 ✅

**技术栈**：React + Vite + TailwindCSS

**8 个页面**：
- HomePage（落地页）、ExplorePage（浏览经验库）、ExperienceDetail（详情）
- UserProfilePage（用户主页）、DashboardPage（控制台）、LeaderboardPage（排行榜）
- PricingPage（定价方案）、SharePage（分享经验）、LoginPage（登录/注册）

**设计风格**：GitHub 式极简（白色/浅灰/深灰），系统字体栈，无字体版权问题

**国际化**：`LocaleContext` + `i18n.js`，中英双语一键切换，所有页面文案全覆盖

### 14.3 v13.0-C 插件社区增强 ✅

**匿名化模块**（`src/community/anonymizer.py`）：
- 脱敏：Windows/Unix 路径、IP、邮箱、手机号、身份证、API Key、Token
- 保留：框架名（React/Vue/Playwright/Appium...）、错误类型、选择器、平台信息
- 评分：0-10 分 7 维度（问题描述+方案质量+根因+代码+标签+长度+平台）
- 审核：拦截空内容、过短、垃圾广告、复制粘贴灌水

**新增后端端点**：
- `POST /community/share/direct` — AI 编程工具直接上传（审核→匿名化→评分→存储）
- `POST /community/share/from-snapshot` — 从已解决调试快照一键分享
- `POST /community/share/preview` — 预览匿名化结果（不写库）
- `POST /community/experiences/{id}/report` — 举报机制（累计 5 次自动标记）

**插件侧对接**（`sidebarProvider.ts`）：
- API 路径和字段名全部对齐新后端
- 分享流程加入预览步骤（先看匿名化结果+评分→确认后提交）
- 测试后自动推荐分享：Bug 修复成功后弹出提示→一键填充分享表单

**测试**：30 个用例全部通过（匿名化 11 + 评分 3 + 审核 8 + 集成 6 + 预览 2）

### 14.4 v13.0-D 部署 + 商业化 ✅

**Docker 部署**：
| 文件 | 作用 |
|------|------|
| `Dockerfile` | API 服务（多阶段构建+健康检查） |
| `web/Dockerfile` | 前端（Node 构建→Nginx 托管） |
| `web/nginx.conf` | API 代理 + WebSocket + SPA 回退 + gzip + 缓存 |
| `docker-compose.yml` | PostgreSQL + API + Web 三服务编排 |
| `.env.example` | 所有环境变量模板 |

**部署命令**：
```bash
cp .env.example .env      # 填入真实密钥
docker compose up -d       # 一键启动
docker compose exec api python -m alembic upgrade head  # 初始化数据库
```

**积分拦截中间件**（`src/billing/middleware.py`）：
- `require_credits(n)` — 余额不足返回 HTTP 402
- `check_plan_limit(feature)` — 套餐无该功能返回 403

**支付 Webhook**（`src/billing/routes.py` 升级）：
- HMAC-SHA256 签名验证
- 支持 `payment.success` / `subscription.renewed` 事件
- 自动升级套餐 + 充值积分
- 套餐升级端点 `POST /billing/upgrade`

**管理后台**（`src/admin/routes.py`）：
- `GET /admin/dashboard` — 系统统计
- `GET /admin/users` + `POST /admin/users/{id}/action` — 用户管理（封禁/解封/改角色/调积分）
- `GET /admin/experiences/flagged` + `POST /admin/experiences/{id}/action` — 举报审核

### 14.5 v13.0 新增文件清单

| 类型 | 文件 | 说明 |
|------|------|------|
| 后端 | `src/community/__init__.py` | 社区模块 |
| 后端 | `src/community/models.py` | 6 张新表 |
| 后端 | `src/community/service.py` | 业务逻辑 |
| 后端 | `src/community/routes.py` | 社区 API |
| 后端 | `src/community/anonymizer.py` | 匿名化+评分+审核 |
| 后端 | `src/billing/plans.py` | 套餐定义 |
| 后端 | `src/billing/routes.py` | 计费 API |
| 后端 | `src/billing/middleware.py` | 积分拦截中间件 |
| 后端 | `src/admin/__init__.py` | 管理模块 |
| 后端 | `src/admin/routes.py` | 管理后台 API |
| 测试 | `tests/test_community_share.py` | 30 个测试 |
| 前端 | `web/` 整个目录 | React 社区门户 |
| 部署 | `Dockerfile` | API 镜像（更新） |
| 部署 | `web/Dockerfile` | 前端镜像 |
| 部署 | `web/nginx.conf` | Nginx 配置 |
| 部署 | `docker-compose.yml` | 生产编排 |
| 配置 | `.env.example` | 环境变量模板 |
| 迁移 | `alembic/` | 数据库迁移 |
| 部署 | `deploy/setup-server.sh` | 服务器初始化脚本 |
| 部署 | `deploy/testpilot-api.service` | systemd 服务 |
| 部署 | `deploy/nginx-testpilot.conf` | Nginx 站点配置 |

---

## 十五、v13.1 服务器部署 + MySQL 适配 (2026-03-15)

### 15.1 服务器信息

| 项目 | 信息 |
|------|------|
| 云服务商 | 腾讯云轻量应用服务器 |
| IP | 82.157.97.179 |
| 配置 | 2C / 2G / 50G SSD / 4Mbps |
| 系统 | Ubuntu 24.04 LTS |
| SSH | 端口 22，密钥登录 |
| 密钥文件 | `key/TestPilotAi.pem`（已在 .gitignore 中排除，不提交） |
| 域名 | testpilot.xinzaoai.com → 82.157.97.179（xinzaoai.com 已备案，二级域名免备案） |

### 15.2 数据库信息

| 项目 | 信息 |
|------|------|
| 类型 | 阿里云 RDS MySQL |
| 主机 | rm-bp1t95uj3l81oe1c8.mysql.rds.aliyuncs.com |
| 端口 | 3306 |
| 数据库名 | testpilot_db（需在 RDS 控制台创建） |
| 字符集 | utf8mb4 |
| 连接URL格式 | `mysql+pymysql://用户名:密码@主机:3306/testpilot_db?charset=utf8mb4` |

### 15.3 代码改动

- `pyproject.toml`：新增 `pymysql` + `cryptography` 依赖
- `src/auth/database.py`：三驱动支持（SQLite / PostgreSQL / MySQL），MySQL 特有 `pool_recycle` + `pool_pre_ping`
- `docker-compose.yml`：移除本地 PostgreSQL 容器，改用外部 DATABASE_URL
- `.env.example`：增加 MySQL 连接字符串示例
- `.gitignore`：排除 `key/`、`*.pem`、`*.key`

### 15.4 部署方式（直接部署，非 Docker）

2GB 内存服务器推荐直接部署而非 Docker，更省资源：

```bash
# SSH 连接
ssh -i key/TestPilotAi.pem ubuntu@82.157.97.179

# 运行初始化脚本
sudo bash /opt/testpilot/app/deploy/setup-server.sh

# 配置环境变量
sudo cp /opt/testpilot/app/.env.example /opt/testpilot/app/.env
sudo nano /opt/testpilot/app/.env

# 初始化数据库
cd /opt/testpilot/app && source venv/bin/activate
python -c "from src.auth.database import init_db; init_db()"

# 启动服务
sudo cp deploy/testpilot-api.service /etc/systemd/system/
sudo cp deploy/nginx-testpilot.conf /etc/nginx/sites-available/testpilot
sudo ln -s /etc/nginx/sites-available/testpilot /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo systemctl enable --now testpilot-api
sudo systemctl restart nginx
```

### 15.5 防火墙端口

| 端口 | 协议 | 用途 | 位置 |
|------|------|------|------|
| 22 | TCP | SSH | 腾讯云防火墙 |
| 80 | TCP | HTTP | 腾讯云防火墙 |
| 443 | TCP | HTTPS | 腾讯云防火墙 |
| 3306 | - | MySQL | 阿里云 RDS 白名单添加 82.157.97.179 |

---

### iOS 真机测试完整验证（2026-03-15）

#### 概述

iOS 真机自动化测试管线全部打通，**52/52 步骤通过，100% 通过率，耗时 116.4 秒**。

#### 验证环境

| 项目 | 值 |
|------|------|
| 设备 | 郑陈钧的iPhone，iOS 16.0 |
| UDID | `00008020-000339A42ED8003A` |
| Appium | v3.2.2 + XCUITest Driver v10.30.0 |
| WDA Bundle ID | `com.wxzao.wda.runner` |
| 签名团队 | Wenzhou Xinzao Technology Co., Ltd（SS9YJ95QFV） |
| 被测应用 | TestPilotDemo（SwiftUI，Bundle ID: `com.testpilot.demo`） |
| 蓝本 | `ios-demo/testpilot/main.testpilot.json`（5场景52步） |

#### 5 个测试场景全部通过

| 场景 | 步骤数 | 结果 |
|------|--------|------|
| 错误密码显示错误提示 | 9 | ✅ |
| 正常登录进入首页 | 9 | ✅ |
| 添加新待办事项 | 13 | ✅ |
| 标记待办完成 | 11 | ✅ |
| 退出登录返回登录页 | 10 | ✅ |

#### 关键修复记录（踩坑）

| 问题 | 根因 | 修复 |
|------|------|------|
| WDA Bundle ID 冲突 | `com.facebook.*` / `io.appium.*` 被占用 | 改为 `com.wxzao.wda.*`（公司专属） |
| 免费开发者 3 app 限制 | Personal Team (VJ35F93D88) 上限 3 个 | 切换到付费团队 SS9YJ95QFV |
| xcodebuild CLI 签名失败 | CLI 拿不到 Apple ID 凭证 | 在 Xcode GUI 中构建 + `usePrebuiltWDA: true` |
| API 路径翻倍 | `_session_request` 自动加 `/session/{id}`，代码又手动加了一次 | 去掉手动前缀，用裸路径 |
| `appium/app/terminate` 返回 404 | XCUITest driver 不支持旧 REST 端点 | 改为 `mobile: terminateApp` via `/execute/sync` |
| App 不重置到登录页 | `activate` 只恢复前台不重启 | `terminateApp` + `launchApp` 冷启动（`@Published` 自动归零） |
| iOS 截图走 adb 崩溃 | adb 仅限 Android | 加 `platform_name != "iOS"` 守卫 |

#### WDA 构建要点

- Xcode GUI 构建（Product → Build），**不要**用 xcodebuild CLI
- DerivedData 路径：`/Users/zcj/Library/Developer/Xcode/DerivedData/WebDriverAgent-cgmjtgwhyundavaoywlwoknvbfhl`
- Capabilities 关键参数：
  ```python
  "appium:usePrebuiltWDA": True,
  "appium:derivedDataPath": "/Users/zcj/Library/Developer/Xcode/DerivedData/WebDriverAgent-cgmjtgwhyundavaoywlwoknvbfhl",
  "appium:xcodeOrgId": "SS9YJ95QFV",
  "appium:updatedWDABundleId": "com.wxzao.wda.runner",
  ```

#### iOS navigate 冷启动实现

引擎 `navigate()` 方法中 iOS 分支：
```python
# mobile: terminateApp → 杀掉 app 进程
_session_request("POST", "/execute/sync", {
    "script": "mobile: terminateApp",
    "args": [{"bundleId": bundle}]
})
await asyncio.sleep(1)
# mobile: launchApp → 冷启动 app
_session_request("POST", "/execute/sync", {
    "script": "mobile: launchApp",
    "args": [{"bundleId": bundle}]
})
await asyncio.sleep(3)  # 等启动渲染
```

#### iOS 蓝本提示词文档

新增 `docs/blueprint-prompt-ios.md`，与 Android/Web/小程序/桌面平行的 iOS 蓝本编写指南。
`copilot-instructions.md` 已新增完整 iOS 蓝本编写规范章节。

#### 遗留的化妆品级警告（不影响测试结果）

- `hide_keyboard` 在 iOS 上产生 404 警告（路径翻倍，化妆品级）
- `adb` UI 树 dump 在 iOS 上尝试失败（正常，iOS 无 adb）
- AI 页面分析 ASCII 编码错误（中文字符，后续修复）

---

## 十六、v13.2 官网上线 + 扩展UI修复 (2026-03-17)

### 16.1 官网部署

| 项目 | 信息 |
|------|------|
| 域名 | https://testpilot.xinzaoai.com |
| SSL | Let's Encrypt，certbot 自动申请，有效期至 2026-06-15，自动续期 |
| Nginx server_name | testpilot.xinzaoai.com（之前被误改为 testpilopai.xinzao.com） |
| 前端构建 | Vite 8.0.0，约 286KB JS，359ms 构建 |

**修复过程**：
1. `nginx server_name` 拼写错误（`testpilopai.xinzao.com` → `testpilot.xinzaoai.com`）
2. 只有 HTTP 80，没有 HTTPS → 安装 certbot + python3-certbot-nginx，自动签发证书并修改 nginx 配置

### 16.2 前端变更

| 文件 | 变更内容 |
|------|---------|
| `web/src/components/Navbar.jsx` | 导航栏加"下载"链接 → `/download` |
| `web/src/pages/DownloadPage.jsx` | 新建下载占位页，含 IDE 插件（VS Code/Windsurf/Cursor）+ 桌面版（Windows/macOS）+ 更新日志 |
| `web/src/App.jsx` | 注册 `/download` 路由 |
| `web/src/lib/i18n.js` | 新增 `nav.download`、`download.subtitle`、`download.changelog`、`download.initialRelease` 中英翻译 |
| `web/src/pages/HomePage.jsx` | 删除首页两张下载卡片 section（迁移到独立下载页） |

### 16.3 扩展按钮状态 Bug（坑23）

**现象**：点击"一键连接"后，按钮应从绿色变为红色"断开"，但一直保持灰色，最终超时恢复绿色

**根因**：原代码用 `delays = [8000, 16000, 25000]` 三个固定时间点检查，用户**多次点击**时，每次点击都生成一套独立的 `setTimeout` 链，旧的 27s 超时 timer 最后反而把已经变红的按钮**改回了绿色**。另外 8s 才第一次检查，引擎 3-4s 就起来了，等待太久。

**修复**：
- 加 `launchTimeoutId` 变量，每次点击先 `clearTimeout` 取消旧 timer
- 改为每 **3s 检查一次**，最多 8 次（24s），引擎通常 3-4s 内响应

**文件**：`extension/src/sidebarProvider.ts`

### 16.4 经验库"无法连接到引擎"（坑24）

**现象**：引擎已连接，切到"经验库"tab 仍显示"⚠️ 无法连接到引擎"，提示文案还写着旧命令 `poetry run python main.py`

**根因**：经验库的 fetch 固定连本地 `127.0.0.1:8900`，本地引擎未启动时直接报错，没有fallback到线上

**修复**：加 `resolveBaseUrl()` 函数，先尝试本地引擎（2s AbortSignal 超时），失败则自动切换到 `https://testpilot.xinzaoai.com/api/v1`

**文件**：`extension/src/sidebarProvider.ts`

### 16.5 部署 SCP 策略说明

因 GitHub 是私有仓库，服务器无访问凭证，采用 **SCP 直传** 方式：
```powershell
$pem = "D:\Projects\TestPilotAI\key\TestPilotAi.pem"
$srv = "ubuntu@82.157.97.179"
scp -i $pem file.jsx "${srv}:/opt/testpilot/app/web/src/pages/file.jsx"
ssh -i $pem $srv "cd /opt/testpilot/app/web && npm run build && sudo cp -r dist/* /var/www/testpilot/"
```

### 16.6 坑25：LoginPage.jsx 末尾重复代码

- **现象**：SCP上传后 `npm run build` 报 JSX 语法错误（Unexpected token）
- **根因**：文件末尾有重复的旧代码段（完整的 `</form>` 到第二个 `}` 都重复了一次）
- **修复**：用 `replace_string_in_file` 删除重复尾部，重新上传重建成功

---

## 17. 版本回归与未提交修复丢失（2026-03-17）

### 17.1 版本对比结论

#### 问题描述
用户反映：3月15日下午4点上传的版本运行正常，但3月17日上传v13.2后感觉"插件不如之前好了"，小程序测试时似乎卡住不停止。

#### Git历史对比

| 版本 | 提交 | 时间 | 说明 |
|------|------|------|------|
| **稳定版** | `dff0933` | 3/15 16:45 | chore: switch domain to testpilot.xinzaoai.com |
| **v13.2** | `8ee17d9` | 3/17 13:32 | HTTPS上线+下载页+导航优化+扩展按钮状态修复+经验库fallback |

#### 关键发现
1. **核心测试引擎代码(src/testing/, src/api/, src/core/)在两个版本之间完全没有改动**
2. v13.2的改动仅限于：
   - `extension/src/sidebarProvider.ts` — 插件UI优化（启动/断开按钮合并、经验库fallback远程服务器）
   - `web/` — 官网页面（下载页、导航、国际化）
   - `src/auth/` — 认证服务
   - `deploy/` — Nginx配置
   - 文档（备忘录、规划）
3. **测试执行流程（runTest/stopTest/testComplete/progress）在diff中完全没有出现**

#### 结论
v13.2的提交本身**不应导致测试卡住**。可能的原因：
- **环境差异**：单位电脑和家里电脑的环境（Python版本、依赖、微信开发者工具版本）不同
- **小程序测试长脚本**：蓝本步骤多时AI调用耗时长，看起来像"卡住"但实际还在跑
- **网络波动**：AI API偶发超时（30秒timeout），多个超时叠加可能导致看起来很久不动

### 17.2 ⚠️ 3月14日未提交的修复已丢失

以下修改在3月14日会话中做了但**未提交到Git**，被v13.2覆盖：

| 修改 | 文件 | 作用 | 状态 |
|------|------|------|------|
| OCR prompt改英文 | `desktop_blueprint_runner.py` | 解决OCR只返回标题"Todo App"的问题 | ❌ 丢失 |
| 弹窗按钮跳过预分析缓存 | `desktop_blueprint_runner.py` | 解决OK/Yes等弹窗按钮用旧坐标点不到 | ❌ 丢失 |
| 弹窗必须关闭规范 | `.windsurf/workflows/blueprint.md` | 蓝本规范新增弹窗铁律 | ❌ 丢失 |
| TodoApp蓝本弹窗关闭步骤 | `todo-demo/testpilot.json` | 蓝本在弹窗后加OK/Yes关闭步骤 | ❌ 丢失 |

**铁律：做完修改必须立即git commit，不能只改不提交。跨会话/跨设备同步全靠Git。**

### 17.3 需要重新实施的修复清单

以下修复需要在下次会话中重新做：

1. **OCR prompt改英文**（`desktop_blueprint_runner.py` `_get_text`方法）
   - 原prompt: `"请识别这张截图中所有可见的文字内容，逐行列出。只输出文字内容本身，不要加任何解释或格式。"`
   - 新prompt: `"List ALL visible text in this screenshot, line by line. Include every button label, list item, input placeholder, status bar text, and title. Output ONLY the raw text, no explanation."`
   - 同时 `reasoning_effort` 从 `"minimal"` 改为 `"low"`，`timeout` 从 20 改为 30

2. **弹窗按钮跳过预分析缓存**（`desktop_blueprint_runner.py` `_smart_tap`方法）
   - 新增类变量 `_DIALOG_BUTTONS = {"name:OK", "name:Ok", "name:Yes", "name:No", "name:Cancel", "name:Close", "name:确定", "name:取消", "name:是", "name:否", "name:关闭"}`
   - `_smart_tap` 开头判断：如果target是弹窗按钮，跳过预分析缓存，直接走UIA→AI视觉实时定位

3. **蓝本规范更新**（`.windsurf/workflows/blueprint.md`）
   - desktop专用注意事项新增：弹窗触发后必须加click步骤关闭弹窗

4. **蓝本弹窗步骤修复**（`todo-demo/testpilot.json`）
   - "重复添加检测"场景截图后加 click name:OK + wait 300
   - "空输入验证"场景截图后加 click name:OK + wait 300

### 17.4 纠正：4项修复并未丢失

经检查当前v13.2代码，备忘录17.2中列出的4项"丢失修复"实际**已存在于当前版本**：
- OCR prompt英文版 ✅（`_get_text`中已是英文prompt + `reasoning_effort="low"` + `timeout=30`）
- 弹窗按钮跳过预分析缓存 ✅（通过AIHub的`DIALOG_BUTTON_KEYWORDS`统一管理）
- 蓝本规范弹窗铁律 ✅（`.windsurf/workflows/blueprint.md`已有）
- 蓝本弹窗关闭步骤 ✅（`todo-demo/testpilot.json`已有`name:OK`步骤）

这些修复在v12.x-v13.x的开发过程中已被合并。

### 17.5 小程序测试"卡住"分析

#### 用户反馈
3月15日安装的插件运行小程序测试顺畅，3月17日v13.2插件运行时感觉卡住不停止。

#### 对比结果
稳定版可能是`5f08b64`(3/13 v11.0-C)，按钮文字为"断开引擎"（红色）。v13.2改为"一键连接/断开"单按钮。

从`5f08b64`到`8ee17d9`(v13.2)，`miniprogram_runner.js`的主要改动是加了`withTimeout`超时包装（来自`1eaf274` iOS真机测试提交）：
- `mp.currentPage()` → 8秒超时，超时返回null
- `mp.evaluate()` → 8-10秒超时
- `mp.screenshot()` → 8秒超时
- `page.callMethod()` → 8秒超时

**潜在风险**：`currentPage()`超时后page=null，后续`page.$(...)`会报错。但这不一定是卡住的根因，也可能是环境差异或偶发网络问题。

#### 结论
暂不修改，直接安装最新插件运行，有错改错。如果再次出现卡住，重点排查：
1. `withTimeout`超时后page=null导致的空指针
2. Node进程异常退出但Python端未检测到
3. 微信开发者工具automator连接不稳定

### 17.6 弹窗自愈机制（三层防御）—— 待实现

详见13.21节的设计。优先级：
- **第2层（检测+自愈）**最重要：步骤失败时截图分析是否有弹窗，自动关闭后重试
- **第3层（场景跳过）**兜底：连续失败N步时跳到下一场景
